# 研错库 Windows 客户端

当前进度：**阶段 E（轻量复习与去重）**。

## 运行

```powershell
cd apps/windows
pip install -e ".[dev]"
python -m yancuo_win
```

## 复习

1. 选中题目 → **加入复习**
2. **今日复习** → 可隐藏答案 → 按 1–5 打分（自动安排下次日期）

间隔：完全不会 1 天 … 完全掌握 14 天。

## 查重

**查重**仅提示哈希重复组与文本相似题，**不会自动删除**。重复原图导入会跳过并提示。
