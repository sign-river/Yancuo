# 全局 UI 视觉与交互重构任务书

> 状态：进行中。每完成一个页面或公共组件，必须更新本文的迁移状态和验收结果。
> 
> 范围：Windows PySide6 客户端。只重构前端视觉、交互与组件组织；不得随意修改数据库、服务接口、同步协议或领域规则。
>
> “自然柔和”组件语言的专项诊断、Qt 技术路径、实施进度与后续计划见 [`24_柔和视觉系统研究与改造计划.md`](24_柔和视觉系统研究与改造计划.md)。

## 1. 目标

将当前 Qt 原生控件拼接式界面，统一为现代、轻量、专业的学习管理产品体验：清晰、克制、长时间使用舒适，并具备明确的信息层级和完整状态反馈。

目标风格参考轻量办公与知识管理工具的设计语言，但不得复制任何第三方品牌、名称、图标、页面或插画。

## 2. 信息架构原则

1. 首页做导航和决策，子页做编辑和执行。
2. 一个页面只服务一个核心用户意图；复杂表单、批量选择器、长列表和多步骤操作必须拆入子页面。
3. 模块结构固定为：模块首页 -> 子任务页 -> 执行页。返回路径必须清晰。
4. 弹窗只用于确认、轻量选择、危险操作或不可中断提醒；完整工作流使用主内容区子页面。
5. 不把所有能力铺在一个纵向页面。桌面端优先使用合理的双栏、三栏、主从列表或详情布局。
6. 空白必须有结构：以容器、栅格、对齐和适度留白建立节奏，不能留下无意义的大块空区域。

## 3. 技术基线

| 项目 | 当前实现 |
| --- | --- |
| 前端技术 | PySide6 / Qt Widgets |
| 页面导航 | `MainWindow` + `QStackedWidget` |
| 全局样式 | `apps/windows/src/yancuo_win/ui/theme.py` |
| 公共组件 | `apps/windows/src/yancuo_win/ui/widgets.py` |
| 图标库 | 暂无。新增图标时必须统一采用同一套线性图标方案，禁止字符模拟图标。 |
| 业务状态 | 页面局部状态 + Application Service；不得为视觉重构改写业务数据模型。 |

## 4. 目标设计系统

### 4.1 颜色

| Token | 值 | 用途 |
| --- | --- | --- |
| app background | `#F5F6F7` | 主内容背景 |
| sidebar background | `#F7F8FA` | 侧栏背景 |
| surface | `#FFFFFF` | 内容表面、卡片、弹窗 |
| surface active | `#E8F0FF` | 当前项、选中行 |
| text primary | `#1F2329` | 主文字 |
| text secondary | `#646A73` | 说明文字 |
| text tertiary | `#8F959E` | 占位与弱提示 |
| border | `#DEE0E3` | 常规边框 |
| border light | `#EFF0F1` | 分隔线 |
| primary | `#3370FF` | 唯一主要操作色 |
| success / warning / danger | `#34A853` / `#F5A623` / `#F54A45` | 状态反馈 |

### 4.2 排版与尺寸

- 字体：`Inter, PingFang SC, Microsoft YaHei, system-ui, sans-serif`。
- 页面标题：20px / 600；区域标题：16px / 600；次标题：14px / 500；正文：14px；辅助文字：12px。
- 间距只使用：4、8、12、16、20、24、32px。
- 输入框与默认按钮：36px 高；紧凑控件：32px。
- 输入框、按钮：6px 圆角；卡片、弹窗：8px；大型区域：8-10px。
- 常规内容区不使用显著阴影；通过背景、边框、间距与对齐建立层级。

### 4.3 组件规范

- 一个操作区域最多一个 Primary Button；次要动作使用默认或文字按钮。
- 所有按钮、输入、下拉、列表、表格、标签、对话框统一由 `theme.py` 控制基础状态：默认、悬停、焦点、禁用、加载、错误。
- 图标按钮必须有 Tooltip；禁止 `×`、`▼`、`...` 等文本模拟图标。
- 列表行需有主标题、次信息、悬停和选中反馈；不使用纯文字堆砌数据。
- 表格不画每单元格完整边框，只保留表头与行分隔。
- 空状态必须包含简短标题、操作说明，必要时给出单个主要操作。

## 5. 应用外壳

1. 左侧导航宽度 220-240px，浅灰背景，当前项浅蓝高亮、深蓝文字。
2. 主内容背景为 `app background`，内边距 24px，内容不贴屏幕边缘。
3. 建立统一 `PageHeader`：返回、标题、说明、面包屑、页面级主要/次要操作。
4. 桌面端大于 1200px 使用完整导航；900-1200px 收窄侧栏和右侧面板；小于 900px 目录与计划草稿使用抽屉。

## 6. 页面模板

| 模板 | 适用页面 | 结构 |
| --- | --- | --- |
| 仪表盘 | 工作台、复习首页 | 概览数据 + 2-5 个任务入口 |
| 文件管理 | 题库、笔记、制定计划 | 目录树 + 内容列表 + 可选详情/草稿侧栏 |
| 列表页 | 计划列表、任务中心、搜索 | 页头 + 工具栏 + 列表/表格 + 批量操作栏 |
| 详情页 | 题目详情、笔记详情 | 页头 + 主内容 + 右侧元数据/操作 |
| 编辑页 | 手动录题、笔记编辑、设置 | 分组表单 + 固定主要提交操作 |
| 沉浸会话 | 题目复习、笔记复习 | 最少干扰的顶部进度 + 内容 + 当前动作 |

## 7. 已完成

- [x] 全局主题 token 第一轮升级：无衬线字体、浅色办公色板、统一输入/按钮/列表/表格/标签页/弹窗基础状态。
- [x] 新增公共组件：`PageHeader`、`EmptyState`、`default_button`。
- [x] 复习模块拆分为任务首页、计划选择、制定计划与执行会话。
- [x] 制定计划第一轮三栏工作区：目录、资料、计划草稿。
- [x] 计划草稿重命名为用户可理解的“计划草稿”。
- [x] 应用外壳第一轮迁移：增加统一顶部模块栏，工作台采用“状态概览 + 任务入口”。
- [x] 公共反馈组件第一轮：`AppHeader`、`SearchInput`、`IconButton`、`StatusTag`、`ConfirmDialog`、`LoadingSkeleton`、`ErrorState`、`BatchActionBar`。
- [x] 制定计划补齐草稿题目总数、空状态、统一确认对话框，以及拖动排序写入计划的业务校验。
- [x] 笔记页第一轮迁移：统一页面头、轻量搜索、资料导航栏与详情工作区样式；保留阅读、编辑、内容块、合集、复习和 AI 录入流程。
- [x] 笔记页信息架构第二轮：合集提升为左侧空间导航，笔记列表独立成中栏，右侧默认阅读；编辑态按内容结构与笔记信息渐进披露。
- [x] 设置页第一轮迁移：统一设置总页与服务/外观子页标题层级，账户、服务、数据与同步继续通过页签集中管理。
- [x] 题目详情与录题流程页第一轮迁移：统一页头和返回入口；详情页分离导航、状态操作与按需 AI 对话，录题保留连续子页面工作流。
- [x] 制定计划页第二轮迁移：启用列表/卡片切换，并为已加入计划草稿的资料提供明确状态和重复加入保护。
- [x] 笔记复习记录：新增 schema v19 的只读笔记完成记录；不使用题目评分和间隔规则。
- [x] 复习会话第一轮迁移：统一复习首页、计划选择与执行会话的页头和返回入口。
- [x] 应用外壳响应式第一轮：顶部导航控制可手动收起侧栏；窗口宽度小于 900px 时自动隐藏侧栏。
- [x] 数据与同步页第一轮迁移：按完整备份、云备份、分享/工作区、索引维护和本地路径重组为双列工作区。
- [x] 低频窗口第一轮迁移：任务中心、重复题检测和原始图片查看器统一标题、空状态、缩放命令和按钮层级。
- [x] 制定计划页窄窗口策略：小于 1080px 时将计划草稿切换为同页专注子视图，保留资料区与草稿区之间的返回路径。
- [x] 全局轻量反馈第一轮：新增 `ToastMessage`，题目和笔记加入当日复习计划使用非阻塞成功提示；错误和需要阅读详情的操作仍保留明确对话框。
- [x] 全量自动化验收：修复默认配置 schema 目标与 v19 不一致的问题；完整单元测试、编译、Ruff 和差异检查通过。
- [x] 审核与今日复习窗口第一轮迁移：统一页头、审核详情区、评分操作和强制覆盖确认行为。
- [x] 题目编辑器第一轮迁移：改为带滚动容器的基本信息、题干、作答与答案、解析与复盘分组表单。
- [x] 桌面视觉验收第一轮：实际启动客户端检查工作台、复习首页和制定计划页；修正侧栏高饱和选中态及禁用主按钮样式。
- [x] 桌面视觉验收第二轮：检查题库、笔记和设置页；修正题库模式切换的高饱和选中态与笔记空状态满宽主按钮。
- [x] 窄窗口回归验收：新增侧栏自动隐藏及计划草稿专注视图切换测试。
- [x] 最终自动化验收：完整单元测试 246 passed，编译、Ruff 与差异检查通过；以最新客户端完成桌面首屏复核。
- [x] 窗口收缩修复：主内容栈不再继承隐藏录题页的最小宽度；主窗口可缩小至 `760×560` 并触发窄窗口布局策略。
- [x] 柔和视觉系统第一轮：增加语义 Surface/Divider/Focus Token、集中曲率指标和 `SoftItemDelegate`。
- [x] 题库与笔记柔和工作区试点：硬分割线改为圆角表面与透明沟槽，树/列表选中态改为内缩圆角绘制。
- [x] 柔和视觉系统第二轮：建立主题感知 SVG 图标服务，替换高频字符箭头与文字模拟菜单。
- [x] 笔记阅读画布：正文居中并限制最大宽度，空笔记状态增加明确的信息与视觉承接。
- [x] 柔和视觉系统第三轮：AI 录题增加三阶段步骤条、分层 Surface 和独立确认操作栏。
- [x] 复习流程柔和迁移：首页、计划选择、计划构建器和执行会话统一表面、沟槽、选择态与阅读画布。
- [x] 柔和视觉系统第七轮：题库富文本行按视口实例化，AI 录题与题目编辑器补齐字段级可访问性，低风险成功反馈继续迁移为 Toast。

## 8. 当前状态与剩余任务

此前清单按“页面整体迁移”记录，导致已完成第一轮或多轮改造的页面仍显示为未开始。
2026-07-30 起只在这里保留可验证的剩余差距；已经形成稳定基线的内容移入第 7 节。

### 已形成的公共基线

- [x] `PageHeader`、`AppHeader`、`SearchInput`、`IconButton`、`StatusTag`、`ConfirmDialog`、`LoadingSkeleton`、`ErrorState`、`BatchActionBar` 已实现并在核心页面接入。
- [x] 主侧栏支持手动收起与窄窗口自动隐藏；笔记和制定计划具有各自的窄窗口渐进披露策略。
- [x] 主题感知线性 SVG 图标服务完成首批高频入口迁移。
- [x] `ToastMessage` 已用于设置保存、索引重建、撤销、加入复习和批量归类等低风险反馈。
- [x] 题库、笔记、复习、录题、设置和首批低频窗口完成柔和 Surface 迁移。
- [x] AI 录题和题目编辑器完成字段级可访问名称与显式 Tab 顺序。
- [x] 题库富文本长列表完成按视口实例化；1200 条隔离资料桌面验收通过。

### P0：发布前质量收口

- [ ] 建立可重复的性能基线脚本，固定数据规模、冷/热启动定义、目标设备和结果记录格式。
- [ ] 检查题目详情、导入导出和同步结果的 Tab 顺序、快捷键提示与读屏朗读质量。
- [ ] 逐项审计剩余 `QMessageBox.information`；仅将短小、可恢复、不含路径或明细的成功反馈迁移为 Toast。
- [ ] 清理仍在使用的页面局部硬编码样式和重复原生按钮组合。
- [ ] 按页面补齐尚缺的加载、失败、禁用和权限不足状态，并为新增状态增加回归测试。

### P1：明确的体验缺口

- [ ] 评估题库是否确实需要独立卡片视图；若保留单列表方案，则从目标规范中删除“列表/卡片切换”要求。
- [ ] 为笔记补充批量移动合集、归档或回收站操作；避免把单篇编辑动作直接复制成批量工具栏。
- [ ] 补充复习计划详情中的内容预览与历史入口；计划构建器的列表/卡片切换、草稿计数、拖动顺序和窄窗口专注视图已完成。
- [ ] 统一题目与笔记复习会话的键盘提示展示；现有阅读画布和评分流程保持不变。

### P2：低频界面第二轮

- [ ] 迁移导入导出、云端恢复/合并结果和历史复习窗口；需要用户记住路径或明细的结果继续使用可停留对话框。
- [ ] 完成剩余字符图标和文本模拟菜单审计，为新增图标补齐普通、悬停、选中和禁用状态。
- [ ] 仅在笔记或复习出现真实富内容长列表性能问题时复用题库按视口实例化方案，纯文本列表不预先复杂化。

## 9. 页面迁移规则

每个页面迁移时必须：

1. 先选择页面模板，再改布局；不能只替换颜色。
2. 使用全局 token 和公共组件；不得新增硬编码颜色、随机边距或局部字体。
3. 保留服务调用、信号、数据字段和业务校验。
4. 添加或保留空、错误、禁用、确认和提交中状态。
5. 在 1366x768 与 1920x1080 下检查文字、按钮、滚动区和分栏不重叠。
6. 为高风险交互补充或更新单元测试。
7. 完成后更新本文“已完成/待迁移”状态。

## 10. 验收标准

1. 所有主要页面第一眼属于同一个产品，且不存在宋体、衬线正文、默认控件或大面积高饱和蓝按钮。
2. 首页仅展示任务入口和状态，复杂操作在子页面完成。
3. 主要页面均有明确主要动作、次要动作、禁用和反馈状态。
4. 题库、笔记和制定计划形成一致的文件管理体验。
5. 不删除或改写既有业务能力：导航、搜索、目录、批量选择、计划草稿、创建计划、复习、答案评分、笔记管理、同步与数据操作。
6. 运行以下检查：

```powershell
cd apps/windows
python -m compileall -q src/yancuo_win/ui
pytest ../../tests/unit -q
python -m ruff check src ../../tests/unit
```

## 11. 更新日志

| 日期 | 内容 | 状态 |
| --- | --- | --- |
| 2026-07-27 | 建立任务书，完成应用外壳、公共组件、题库/笔记/复习/设置和低频窗口第一轮迁移 | 已完成 |
| 2026-07-29 | 完成柔和视觉系统、主题化图标、阅读画布、响应式布局与亮暗主题/系统缩放验收 | 已完成 |
| 2026-07-29 | 完成键盘焦点、快捷键、字段读屏标签和低风险 Toast 第一轮迁移 | 已完成 |
| 2026-07-29 | 完成 1200 题/300 笔记性能采样与题库富文本行按视口实例化，完整测试 `286 passed` | 已完成 |
| 2026-07-30 | 依据当前实现重整任务状态，移除已完成但仍显示未开始的重复条目 | 已完成 |
