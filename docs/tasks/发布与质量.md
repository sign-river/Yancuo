# 发布与质量分任务书

> 负责可重复验收、安装交付、导出和安全增强；不重复维护各业务方向的功能任务。

## 已完成

### REL-000 · 总分任务书与问题流转体系

- 状态：已完成
- 来源问题：`ISSUE-20260730-001`
- 结果：建立问题收集箱、总任务书、四个方向分任务书和单一完成记录；合并 schema 与 UI 重复文档，根 README 只保留稳定入口和当前基线。
- 验收：旧文档引用清零，本地 Markdown 链接检查通过。

### REL-001 · 可重复性能基线

- 状态：已完成
- 数据规模：至少 1200 道题目和 300 篇笔记，全部使用隔离资料。
- 指标：冷启动、热启动、主窗口构建、题库刷新、搜索、列表查询和滚动。
- 记录：目标设备、Python/Qt 版本、数据生成方式、采样次数、中位数和异常值。
- 验收：一条命令生成隔离数据、执行采样并清理；不得接触用户正式资料。
- 结果：新增 `python tools/performance_baseline.py --samples 5`；脚本只在系统临时目录生成带隔离标记的资料，报告环境、原始样本、中位数、范围和 Tukey 异常值，退出前关闭日志并完整清理。

## 当前任务

### REL-002 · Windows 正式安装包

- 状态：已完成
- 范围：安装、升级、卸载、用户数据保留、默认配置和协议资源。
- 验收：干净 Windows 环境安装启动，升级不覆盖数据，卸载不误删用户资料。
- 结果：新增 PyInstaller + Inno Setup 每用户安装包、安装态资源/迁移自检、隔离的安装—覆盖升级—卸载数据保护脚本，以及版本标签/手动触发的 Windows 构建工作流；本机完整回归通过。

### REL-003 · PDF 导出

- 状态：后置
- 当前基线：Word 是稳定导出路径。
- 启动条件：明确排版、公式、图片、分页和字体嵌入标准。

### REL-004 · `.ebpack` 端到端加密

- 状态：后置
- 当前基线：仅支持未加密 `.ebpack`。
- 启动条件：完成密钥派生、版本协商、错误恢复和跨端测试向量规范。

### REL-005 · 对象库路径边界加固

- 状态：已完成
- 来源：全部可执行任务完成后的主动安全审计。
- 风险：资源相对路径来自数据库、同步或导入记录；旧 `ObjectStore.resolve` 只拼接并解析路径，没有确认最终目标仍位于内容寻址对象库。恶意或损坏记录可能借助 `..`、绝对路径、驱动器路径或符号链接把读取重定向到对象库之外，并被预览、导出或 AI 图片上下文读取。
- 修复：统一拒绝空路径、NUL、绝对路径、驱动器/ADS 冒号和解析后越出对象库的目标；`objects/` 前缀与旧的无前缀相对格式均限制在同一 resolved root 内，指向根目录本身也被拒绝。
- 验证：覆盖正常跨端哈希对象、父目录穿越、伪造 `objects/../../`、Windows 绝对路径、空路径和目录目标，并回归资源服务、增量同步和题目讨论裁剪读取。

### REL-006 · 内容寻址对象写入完整性

- 状态：已完成
- 来源：对象库路径边界修复后的连续安全审计。
- 风险：旧写入流程在目标哈希路径已存在时直接信任文件，损坏或被替换的对象会继续作为正确原图使用；新对象也直接复制到最终文件名，进程中断可能留下可见的半写入文件。
- 修复：复用既有对象前重新计算 SHA-256 并拒绝哈希不符；新对象先写入同目录唯一临时文件、刷新并 `fsync`、复核哈希后再原子替换到最终路径。写入目标、父目录或既有目标为越界符号链接时拒绝操作，临时文件在所有失败路径清理。
- 验证：覆盖损坏的既有哈希路径拒绝、临时写入后内容一致、无临时文件残留，以及录题和资源服务回归。

### REL-007 · 外部编辑工作区读取边界

- 状态：已完成
- 来源：外部输入面连续安全审计。
- 风险：工作区导入会读取用户选择目录中的 manifest、题目 Markdown、元数据和资源引用；旧流程允许题目目录/文件符号链接，资源文件名也可包含父目录穿越，且文本读取没有单文件或题目目录数量上限。
- 修复：manifest、元数据和 Markdown 统一使用带 UTF-8、符号链接及大小限制的读取器；`problems/`、题目目录、`assets/` 和资源文件拒绝符号链接，资源名通过安全相对路径解析；题目目录总数限制为 10000。
- 验证：覆盖正常导出—导入—审核、缺失资源、无效 manifest，以及指向工作区外既有文件的 `../../` 资源引用拒绝。

### REL-008 · 增量同步批次输入预算

- 状态：已完成
- 来源：云端与本地同步输入面连续安全审计。
- 风险：GitHub 批次索引中的远端 `asset_name` 曾直接参与本地临时路径拼接；本地文件夹与 GitHub 的 JSONL 批次读取也缺少文件大小、记录数和设备/批次数量预算，损坏或恶意同步目录可造成路径越界尝试或内存/磁盘消耗。
- 修复：GitHub 只接受固定 `operations.jsonl` 资源名并始终下载到本地固定文件名，同时校验 SHA-256 格式、批次 ID 去重、索引/文件/记录上限与 UTF-8；本地文件夹通道拒绝 `ops.jsonl` 符号链接，并使用相同量级的设备数、文件大小、记录数和编码预算。
- 验证：覆盖 GitHub 正常推拉、篡改哈希、错误索引、恶意 `../../` 资源名不触发下载，以及本地 JSONL 非对象行、超限文件和非法 UTF-8。

### REL-009 · 单资源存储预算

- 状态：已完成
- 来源：资源耗尽风险连续审计。
- 风险：图片/附件在计算哈希和复制前没有统一单文件大小限制，手动选择或文件夹扫描到异常大文件时会长时间占用磁盘 I/O，并可能在预览、导出或 AI 上下文阶段进一步放大内存占用；空文件也会进入内容寻址库成为无效资源。
- 修复：对象库作为所有原图和派生资源的统一写入边界，在哈希前拒绝空文件和超过 128 MiB 的单文件；所有录题、笔记、分享包、同步附件和派生题图自动继承同一预算。
- 验证：覆盖空文件、超限文件、正常原图、派生资源和录题/资源服务回归。

### REL-010 · 本地文件夹云元数据完整性

- 状态：已完成
- 来源：可移动盘、网络盘和同步盘输入面连续安全审计。
- 风险：`repository.json`、`latest.json`、`release.json` 与 `devices.json` 来自可被其他进程修改的共享目录；旧流程无统一大小/编码/JSON 类型限制，release、asset 和 device 列表也无数量预算，部分写入直接覆盖目标文件，异常中断可能留下半写入元数据。
- 修复：共享 JSON 元数据统一拒绝符号链接、超 8 MiB、非法 UTF-8/JSON 和错误根类型；release、单 release 资源及设备列表分别限制为 10000 项。同步清单和设备列表使用同目录唯一临时文件、刷新、`fsync` 和原子替换，失败后清理临时文件；设备 ID 通过安全路径组件校验。
- 验证：覆盖正常清单往返、原子写入无临时文件残留、超限读取拒绝、不安全设备 ID 拒绝，以及云备份、资料合并与增量同步回归。

### REL-011 · AI 审核批量决策查询去重

- 状态：已完成
- 来源：全部可执行任务完成后的性能与冗余审计。
- 问题：AI 补全审核列表刷新时已经取得每条建议的并发状态，但“全部采用”仍按字段重复调用展示查询；同一建议含多个字段时会重复解析审核载荷并访问题目状态，形成随字段数增长的 N+1 查询。
- 优化：列表行复用刷新阶段取得的状态快照，批量决策直接判断该快照并继续跳过冲突字段；最终刷新仍重新读取服务端状态，保持界面与实际审核项一致。
- 验证：新增批量决策查询次数回归，完整 Windows 单元测试 `409 passed`，Ruff 与 Python 编译检查通过。

### REL-012 · 朋友分享包完整性与输入预算

- 状态：已完成
- 来源：外部归档输入面连续安全审计。
- 风险：`.gmshare` 导入只校验 checksum 表中主动列出的文件，缺失条目不会失败，因此题目载荷、资源索引或额外文件可能绕过完整性覆盖；题目 JSONL 也会一次性读入内存，且 manifest、记录数、资源引用数和单行大小缺少一致预算。
- 修复：checksum 表必须以规范相对路径完整且唯一地覆盖包内除自身外的全部文件，摘要必须为 SHA-256；题目 JSONL 改为逐行限长读取，题目与资源引用均限制为 10000 条，并交叉核对 manifest、资源索引和题目引用数量。manifest、题目记录与资源索引同时校验 JSON 根类型。
- 验证：覆盖正常脱敏导入和溯源去重、题目载荷被篡改、checksum 覆盖缺失、归档安全边界；完整 Windows 单元测试 `411 passed`，Ruff 与 Python 编译检查通过。

### REL-013 · AI 网络响应内存预算

- 状态：已完成
- 来源：外部网络输入面连续安全审计。
- 风险：OpenAI 兼容接口的普通 JSON、非 SSE 回退和流式 SSE 响应此前都没有累计读取上限；配置错误、异常代理或恶意兼容服务可持续返回数据并耗尽客户端内存，HTTP 错误正文也会先完整读取再截断展示。
- 修复：普通与流式成功响应统一限制为 16 MiB，SSE 在逐行处理时累计字节并立即终止超限连接；错误正文最多读取 4 KiB。响应解析、密钥脱敏、瞬态重试和已输出流式内容不自动重试的语义保持不变。
- 验证：新增普通 JSON 与 SSE 超限拒绝回归，完整 Windows 单元测试 `413 passed`，Ruff 与 Python 编译检查通过。

### REL-014 · GitHub 认证源站与下载预算

- 状态：已完成
- 来源：云端网络边界连续安全审计。
- 风险：GitHub Release 返回的上传/下载 API URL 会携带 Bearer 令牌继续请求，但此前未限制目标源站；异常 API 响应可把令牌带到第三方地址。GitHub JSON 响应和 Release 附件实际下载量也没有读取上限，远端声明大小不能单独作为可信预算。
- 修复：GitHub API 基址强制为 HTTPS，所有认证请求仅允许同 API 源站；官方 `api.github.com` 额外只放行 `uploads.github.com` 上传源站。JSON 响应限制为 8 MiB、错误正文限制为 4 KiB；下载同时校验声明大小并逐块执行 2 GiB 实际上限，超限删除部分文件。
- 验证：覆盖跨源认证 URL 拒绝、JSON 超限、附件实际下载超限与清理，以及 GitHub Release/Operation 同步回归；完整 Windows 单元测试 `416 passed`。

### REL-015 · CloudBase 网关传输边界

- 状态：已完成
- 来源：云端网络边界连续安全审计。
- 风险：CloudBase 网关配置和返回的签名存储地址接受明文 HTTP；网关 JSON 和对象下载会一次性无界读入内存，上传也没有统一文件预算。异常网关或存储端可造成令牌/资料明文传输、内存耗尽或超量落盘。
- 修复：网关与签名存储 URL 强制为无内嵌凭据的 HTTPS；网关 JSON 限制为 8 MiB、错误正文限制为 4 KiB，上传与下载统一限制为 512 MiB。下载改为逐块写入并累计实际字节，所有失败路径删除部分文件，并通过 provider capability 公布相同上限。
- 验证：覆盖明文网关拒绝、网关响应超限、对象实际下载超限与部分文件清理，以及 CloudBase 工厂和健康检查回归；完整 Windows 单元测试 `419 passed`。

### REL-016 · HTTPS 重定向凭据隔离

- 状态：已完成
- 来源：认证网络请求连续安全审计。
- 风险：Python 默认 HTTP 重定向会复制 `Authorization` 等请求头；AI、GitHub 或 CloudBase 的服务端若返回跨源 30x，可把 API Key、Bearer 令牌或存储授权头转发到第三方地址，HTTPS 到 HTTP 的降级跳转也会被自动跟随。
- 修复：新增统一安全 HTTPS 打开器。认证 API 默认只跟随同源 HTTPS 重定向；GitHub 附件下载因官方对象存储跳转需要可选择允许跨源 HTTPS，但在跳转前移除 Authorization、Cookie、代理授权与 API Key 头。所有 HTTP 降级跳转一律拒绝，AI、GitHub 和 CloudBase 的网络入口统一接入。
- 验证：覆盖同源保留认证、默认拒绝跨源、必要跨源剥离认证和 HTTPS 降级拒绝，并回归三个 provider；完整 Windows 单元测试 `423 passed`。

### REL-017 · GitLink 附件路径与传输预算

- 状态：已完成
- 来源：GitLink 完整快照通道连续安全与冗余审计。
- 风险：Release 资源名在进入上传器的安全文件名处理前，会先作为本地临时文件名拼接并复制，路径型名称可越出源文件目录；公开 Release 回退响应和附件下载也没有实际读取预算，provider 未公布资源上限。
- 修复：资源名在任何使用前拒绝空值、父目录、控制字符、分隔符和 NTFS ADS 冒号；移除完全不需要的同名临时复制，原文件直接流式上传并单独传递安全远端名称。公开列表限制为 8 MiB，上传/下载限制为 512 MiB，下载逐块累计实际大小并在失败时删除部分文件，同时公布 capability 上限。
- 验证：覆盖路径型资源名不触发上传或越界文件、公开响应超限、实际下载超限与清理，以及 Attachment/Release 和快照同步回归；完整 Windows 单元测试 `426 passed`。

### REL-018 · 云端大文件流式上传

- 状态：已完成
- 来源：云备份内存占用连续性能审计。
- 问题：GitHub 与 CloudBase 虽已限制附件容量，但上传前仍调用 `read_bytes()` 把整个备份包读入内存；接近 512 MiB 的正常快照会额外占用同等内存，GitHub 突发重试时还会重复分配大字节串。
- 优化：新增可重入的 1 MiB 文件块迭代器，GitHub 每次重试都创建新的流并显式发送 `Content-Length`，CloudBase 签名 PUT 同样直接迭代源文件；两端不再构造完整上传字节串，既有容量限制、内容类型和响应校验保持不变。
- 验证：捕获实际 Request body 证明 GitHub 与 CloudBase 均以迭代器发送完整内容和正确长度，完整 Windows 单元测试 `428 passed`。

### REL-019 · AI 图片请求内存预算

- 状态：已完成
- 来源：AI 图片输入连续性能与资源耗尽审计。
- 风险：普通和流式视觉请求各自重复实现图片 Base64 编码，并会对任意数量、任意大小的路径直接 `read_bytes()`；单个 128 MiB 对象编码后会膨胀到约 171 MiB，多图请求可在发网前耗尽内存。
- 修复：普通与流式路径统一复用一个受限编码器；每次最多 20 张、单张 32 MiB、原始图片合计 64 MiB。读取前后同时核对文件大小，拒绝空文件、读取期间变化和超限输入，再生成 Base64 数据 URI。
- 验证：覆盖超限图片在网络调用前拒绝，并回归单图、多图、普通与流式结构化请求；完整 Windows 单元测试 `429 passed`。

### REL-020 · 题目对话原图上下文预算

- 状态：已完成
- 来源：题目 AI 对话图片上下文连续资源耗尽审计。
- 风险：用户授权附带原图后，对话服务会把题目全部原图逐个完整读取并 Base64 编码；它位于 provider 请求预算之前，因此大量或超大历史原图仍可先造成内存膨胀。
- 修复：题目对话原图上下文与视觉结构化请求采用相同量级边界：最多 20 张、单张 32 MiB、原始数据合计 64 MiB；读取前后核对大小，空文件、超限或变化中的对象会让当前消息明确失败且不调用 AI。
- 验证：覆盖有序多原图、无图文字降级和超限原图在 provider 前拒绝，完整 Windows 单元测试 `430 passed`。

### REL-021 · 富题干题图渲染预算

- 状态：已完成
- 来源：富内容列表和阅读页连续内存审计。
- 风险：题库行、题目详情、今日复习和复习会话会把结构化题图完整读入并转成 Base64 data URI；单个损坏或异常大的派生图即可在 Qt HTML 渲染前造成显著内存膨胀，同一题的多图也没有累计预算。
- 修复：题图物化最多 50 张、单张 16 MiB、原始数据合计 32 MiB；读取前后核对大小，空文件、超限文件和读取期间变化的文件保持为带题注的安全占位，不进入 Base64。正常小图渲染及内容块数据不变。
- 验证：覆盖小图 data URI 内容一致和超限图安全跳过，并回归题目详情、复习阅读和服务层；完整 Windows 单元测试 `432 passed`。

### REL-022 · 增量同步题图读取预算前置

- 状态：已完成
- 来源：增量 Operation 附件连续内存审计。
- 风险：结构化题图附件虽有 32 MiB 累计限制，但旧流程先对每个对象调用 `read_bytes()`，再累加并判断；单个 128 MiB 对象仍会完整进入内存后才失败，使预算无法防止峰值占用。
- 修复：每个附件读取前先按剩余预算检查实际文件大小，再最多读取“剩余预算 + 1”字节并核对读取前后大小；超限、空文件或读取期间变化均在 Base64 和 Operation 入库前失败，SHA-256 与缺失引用检查保持不变。
- 验证：覆盖把预算缩小到对象以下时在附件生成阶段拒绝，并回归本地/远端结构化题图同步；完整 Windows 单元测试 `433 passed`。

### REL-023 · Operation 附件累计预算校验

- 状态：已完成
- 来源：远端 Operation 协议连续资源耗尽审计。
- 风险：协议只限制附件数量和每个 Base64 字符串长度，没有限制解码后附件累计大小，也接受空附件；直接构造或未来放宽批次上限时可让一次校验保留大量已解码字节，且生产端与校验端的 32 MiB 约束分散。
- 修复：把 32 MiB Operation 附件预算提升为领域协议常量，由生产端读取和接收端校验共同使用；校验逐项累计实际解码字节并拒绝空内容或总量超限，既有数量、Base64、声明大小、MIME 和 SHA-256 规则保留。
- 验证：覆盖接收端累计预算拒绝，并回归 Operation 形状校验与结构化题图推拉；完整 Windows 单元测试 `434 passed`。

### REL-024 · AI API 初始连接凭据保护

- 状态：已完成
- 来源：AI 认证网络入口连续安全审计。
- 风险：安全重定向已拒绝 HTTPS 降级，但 OpenAI 兼容 provider 的初始 Base URL 仍接受明文 HTTP；直接调用模型列表或请求方法也不一定先经过设置页连接校验，API Key 可能在首跳以明文发送。URL 中还可内嵌用户信息、查询令牌或片段，增加凭据落日志风险。
- 修复：所有普通、流式和模型列表请求在读取 API Key 前统一验证 Base URL，仅接受有主机且不含内嵌凭据、查询参数或片段的 HTTPS 地址；不仅依赖设置页的显式测试流程。
- 验证：分别覆盖 HTTP、userinfo 和查询参数地址均在网络前拒绝，并回归普通与流式 Faro 请求；完整 Windows 单元测试 `437 passed`。

### REL-025 · 当日复习日期网络去重

- 状态：已完成
- 来源：复习计划批量操作连续性能与隐私审计。
- 问题：每次把题目或笔记加入当日计划都会向固定站点发起最长 2 秒的 HEAD 请求；批量加入题目时位于逐题循环中，离线环境可能按题数重复等待，也会产生不必要的外部网络访问。网络日期头异常还可能越过原有 `OSError` 捕获。
- 优化：网络确认日期和上海本地降级结果统一缓存 5 分钟，批量与连续操作复用一次结果；请求接入安全 HTTPS 重定向边界，并完整处理 HTTP、URL、日期解析和本机 I/O 异常。
- 验证：覆盖连续两次日期查询只发生一次网络调用且跨 UTC 日正确转换为上海日期，完整 Windows 单元测试 `438 passed`。

### REL-026 · 本地身份文件完整性

- 状态：已完成
- 来源：本地持久化与云资料身份连续安全审计。
- 风险：`identity.json` 决定用户、设备、数据库和资料谱系身份，但此前无大小、JSON 根类型、标识符或符号链接校验，并直接覆盖写入；异常中断可留下半写文件，损坏或导入的路径型 ID 也会进入同步与云端流程。
- 修复：身份文件限制为 64 KiB，拒绝空文件、符号链接、非对象根、路径型/超长 ID、无效 profile/snapshot 和超长显示字段；读取前后核对大小。创建、谱系绑定和快照头更新统一使用同目录唯一临时文件、刷新、`fsync` 与原子替换，失败后清理临时文件。
- 验证：覆盖首次创建与持久化、旧身份自动补 profile、原子写无临时残留、超限文件、错误根类型和路径穿越 ID，并回归快照云恢复；完整 Windows 单元测试 `441 passed`。

### REL-027 · 用户偏好文件统一持久化边界

- 状态：已完成
- 来源：本地配置连续安全与冗余审计。
- 风险：AI、云端、主题和阅读缩放四个保存函数各自重复“读取 JSON—固定 `.tmp`—替换”逻辑；读取无大小/符号链接限制，固定临时名可冲突，写入未刷新或 `fsync`。损坏旧文件还会被静默清空并覆盖，导致其他偏好丢失。
- 修复：统一为一个 1 MiB、UTF-8、对象根和符号链接受限的读取器，以及同目录唯一临时文件、刷新、`fsync`、二次符号链接检查和原子替换的写入器；四类偏好共用该边界，损坏文件保存时明确报错并保留原内容。
- 验证：覆盖各类偏好互相保留、无凭据落盘、原子写无临时残留、超限读取及损坏文件不覆盖；完整 Windows 单元测试 `443 passed`。

### REL-028 · 本地文件夹附件复制边界

- 状态：已完成
- 来源：共享目录文件操作连续安全审计。
- 风险：LocalFolder 上传使用可预测的 `.<asset>.uploading` 临时名并由 `copy2` 直接打开，预置符号链接可把写入重定向到共享目录外；下载会跟随远端附件符号链接并直接覆盖目标，既可能读取库外文件，也可能写入目标符号链接。上传、下载和 capability 均无容量预算。
- 修复：上传和下载拒绝源/目标符号链接，统一限制为 512 MiB；使用短名称的同目录唯一临时文件逐块复制、核对读取前后大小、刷新、`fsync` 后原子替换，所有失败路径删除部分文件。短临时名前缀同时避免 Windows 深层 Release 路径超长。
- 验证：覆盖正常原子往返、无临时残留、超限上传、符号链接下载源拒绝，以及云备份、恢复与资料合并；完整 Windows 单元测试 `445 passed`。

### REL-029 · 本地文件夹 Release 元数据原子化

- 状态：已完成
- 来源：共享目录元数据连续完整性审计。
- 风险：仓库描述、Release 描述和 tombstone 仍直接覆盖写入，异常中断可留下半写 JSON；Release 名称/正文及 tombstone 也绕过 8 MiB 元数据预算。列表会把 Release 目录中的符号链接和崩溃残留上传临时文件当作普通附件暴露。
- 修复：所有本地文件夹 JSON 写入统一经过 8 MiB 检查、唯一临时文件、刷新、`fsync` 和原子替换；创建 Release 若元数据失败会移除空目录。Release 列表拒绝符号链接附件并忽略仍在写入的 `.up-*.tmp`，不暴露库外路径或半成品。
- 验证：覆盖仓库、Release 和 tombstone 正常写入无临时残留，超限 Release 不留下目录，并回归云备份、恢复和 Operation 同步；完整 Windows 单元测试 `446 passed`。

### REL-030 · 本地 Operation 日志写入与流式读取

- 状态：已完成
- 来源：共享增量日志连续持久化与内存审计。
- 风险：LocalFolder `ops.jsonl` 追加前不检查单行、批次或写后文件大小，也不刷新/`fsync`；写到一半失败会留下可与下一批拼接的残行。读取虽然检查 64 MiB 文件大小，却用 `read_text().splitlines()` 同时保留整文件和行列表，且数量预算只在完整分割后生效。
- 修复：追加前编码并校验对象、记录数、48 MiB 单行和 64 MiB 写后总量，二进制完整写入后 `fsync`；失败时按原长度截断并同步，回滚失败单独报错。读取改为限长逐行 UTF-8/JSON 解析，跨设备按物理行累计最多 100000 条，继续兼容跳过损坏或非对象行。
- 验证：覆盖超限追加在写入前拒绝且原文件逐字节不变、损坏行兼容、超限文件/编码拒绝，以及 LocalFolder 与 GitHub Operation 回归；完整 Windows 单元测试 `447 passed`。

### REL-031 · 视觉引用解码与请求预算

- 状态：已完成
- 来源：题目 AI 对话视觉引用连续资源耗尽审计。
- 风险：单条消息的框选引用数量没有上限；裁剪前直接解码原图，小体积高像素图可以造成大量内存分配，多个裁剪后的 PNG 也会无界累积到 AI 请求。
- 修复：发送和重建持久化快照时均限制单消息最多 20 个引用；先用图像头部尺寸拒绝超过 2500 万像素的源图，再解码和裁剪，并限制单条消息的裁剪 PNG 原始数据合计为 32 MiB。
- 验证：覆盖超量引用在持久化前拒绝、高像素源图在 AI 请求前失败，以及正常框选顺序、快照与裁剪回归；完整 Windows 单元测试 `449 passed`。

### REL-032 · ebpack 元数据读取预算

- 状态：已完成
- 来源：备份包外部输入面连续内存审计。
- 风险：ZIP 通用安全边界允许单成员最大 256 MiB，但 ebpack 的 `manifest.json`、`checksums.sha256` 和 `assets/index.json` 仍一次性读取且没有更紧的元数据上限；恶意包可在 JSON 解析或分行前占用大量内存。
- 修复：三类 ebpack 元数据统一限制为 8 MiB；ZIP 内 manifest 先检查声明大小再限量读取，解压后文件拒绝符号链接、核对读取前后大小并限量读取。非法 UTF-8/JSON 统一转换为领域错误。
- 验证：覆盖超限 ZIP manifest 在 JSON 解析前拒绝、超限已解压 checksum 表在分行前拒绝，并回归 ebpack 完整性、Schema 和往返恢复；完整 Windows 单元测试 `451 passed`。

### REL-033 · ebpack 导出原子交付

- 状态：已完成
- 来源：备份导出连续持久化与并发审计。
- 风险：导出此前按秒构造固定 staging 目录，同秒并发会相互删除中间数据；打包前先删除已有目标并直接向最终路径写 ZIP，中途失败会丢失上一份有效备份且留下半成品。
- 修复：每次导出使用唯一 staging 目录；ZIP 先写入目标同目录唯一临时文件，关闭归档、刷新并 `fsync` 后再原子替换最终路径。所有失败路径清理临时归档和 staging。
- 验证：模拟 ZIP 成员写入中断，旧备份逐字节保留且目标/缓存目录无临时残留，并回归 ebpack 导出、校验与恢复；完整 Windows 单元测试 `452 passed`。

### REL-034 · ebpack SQLite WAL 一致性快照

- 状态：已完成
- 来源：备份内容一致性连续审计。
- 风险：ebpack 导出此前在释放 SQLAlchemy 连接后直接复制 `error_book.db`；SQLite 处于 WAL 模式且仍有连接时，已提交修改可能只存在 `-wal` 中，文件复制会产生可打开但丢失最新数据的备份。数据库与身份文件也可被数据根中的符号链接重定向。
- 修复：使用 SQLite Online Backup API 生成一致性 `snapshot.sqlite`，同时合并主库与 WAL 的已提交页，再在快照上移除可重建的 FTS 虚拟表。数据库源和可选身份文件均拒绝符号链接。
- 验证：保持独立 WAL 写连接并禁用自动 checkpoint，提交题目修改后立即导出，包内快照能读取最新值；完整 Windows 单元测试 `453 passed`。

### REL-035 · 本地 ZIP 备份一致性与原子交付

- 状态：已完成
- 来源：传统本地备份路径与 ebpack 连续对照审计。
- 风险：`create_backup` 同样直接复制 SQLite 主库，会遗漏 WAL 中已提交修改；ZIP 直接写最终路径，中途失败会覆盖旧备份并留下半成品。身份文件也未拒绝符号链接。
- 修复：提取共用 SQLite Online Backup 快照服务，本地 ZIP 与 ebpack 均从主库/WAL 生成一致性快照；备份 ZIP 在目标同目录唯一临时文件中完整写入、`fsync` 后原子替换，失败时清理唯一 staging 与临时归档。身份源符号链接被拒绝。
- 验证：覆盖未 checkpoint 的已提交 WAL 修改进入备份，以及 ZIP 写入中断后旧备份逐字节不变、无临时残留；完整 Windows 单元测试 `455 passed`。

### REL-036 · 本地备份恢复元数据与临时目录边界

- 状态：已完成
- 来源：本地 ZIP 备份恢复连续外部输入与并发审计。
- 风险：恢复此前使用 ZIP 通用的 256 MiB 单成员预算一次性读取 `manifest.json`；恶意包可在 JSON 解析前占用大量内存。解压、最终 staging 和旧数据保留目录名称固定，并会在开始时删除同名目录，重入或并发恢复可破坏另一流程的中间数据。
- 修复：归档基础层新增通用限量 ZIP 成员读取器，同时校验声明大小与实际读取量；本地备份 manifest 限制为 1 MiB，ebpack 也复用该边界。三类恢复临时目录改为目标根内每次唯一名称，不再预删除其他流程的目录。
- 验证：覆盖超限 manifest 在 JSON 解析/解压前拒绝，且失败后所有唯一恢复目录均清理；回归本地备份恢复、ebpack 元数据与安全归档流程，完整 Windows 单元测试 `456 passed`。

### REL-037 · 备份身份文件语义一致性

- 状态：已完成
- 来源：备份包中数据库与本地身份关联的连续完整性审计。
- 风险：本地 ZIP 与 ebpack 此前只把 `identity.json` 当作普通文件复制；启动后被替换的无效/异库身份会进入备份，恢复也会在未验证 ID 和 manifest 关系时覆盖目标身份，造成资料库、用户、设备或云资料绑定错位。
- 修复：领域层新增不修改文件的 `read_identity`，复用 64 KiB、UTF-8、符号链接和 ID 格式校验。导出要求盘上身份的用户/设备/资料库与运行时一致，并以运行时快照写入归档，兼容同步流程中已确认但尚未落盘的 profile 绑定。恢复/验包要求 identity 与 manifest 的 database/profile ID 一致。
- 验证：覆盖只读验证不改写旧身份、启动后异库身份不生成备份、包内身份与 manifest 不同时在替换目标前失败，并回归 GitHub 跨设备 profile 同步；完整 Windows 单元测试 `459 passed`。

### REL-038 · 本地 ZIP 备份载荷完整性

- 状态：已完成
- 来源：传统本地备份介质损坏与静默数据错误连续审计。
- 风险：本地 ZIP 备份只依赖 ZIP CRC 和恢复后的 SQLite 核心表检查；资源文件损坏、遗漏或额外载荷不参与业务层完整性校验，可被静默恢复并在后续预览、AI 或同步时才暴露。
- 修复：本地备份升级为 v2，manifest 完整列出数据库快照、身份和所有资源载荷的 SHA-256；恢复在复制/替换目标前比对规范哈希、载荷集合与实际文件内容。继续支持无 checksum 的旧 v1 备份，v2 manifest 预算扩展为 4 MiB 以容纳最大资源索引。
- 验证：篡改包内资源但保留 manifest 后，恢复在目标替换前以 checksum 不匹配失败，旧数据库逐字节不变；回归原子导出、WAL 快照、身份交叉校验与正常恢复，完整 Windows 单元测试 `460 passed`。

### REL-039 · gmshare 元数据与物理行预算

- 状态：已完成
- 来源：朋友分享包连续资源耗尽与冗余读取审计。
- 风险：`.gmshare` 的 manifest 与资源索引在安全解压后仍使用无业务上限的 `read_text`，可一次性加载通用 ZIP 边界允许的大文件。checksum 和题目 JSONL 只对有效记录计数，大量空白/注释物理行可绕过数量预算并消耗 CPU/I/O。
- 修复：归档基础层新增普通文件限量读取器，统一拒绝符号链接、超限大小和读取期间变化；gmshare manifest 与资源索引均限制为 8 MiB，ebpack 解压后元数据也复用该实现。checksum 与 problems JSONL 均按物理行累计最多 20000 行。
- 验证：覆盖超限 manifest 在 JSON 解析前失败，以及空白 checksum 行同样消耗物理行预算；回归 gmshare 脱敏、溯源去重、checksum 完整覆盖与 ebpack 元数据，完整 Windows 单元测试 `462 passed`。

### REL-040 · gmshare 原子导出与唯一工作目录

- 状态：已完成
- 来源：朋友分享包连续持久化与并发审计。
- 风险：导出 staging 按秒生成固定名称，同秒任务会相互删除中间载荷；导入 staging 只取包文件名，同名包重入/并发时同样会相互删除。ZIP 打包先删除旧目标并直接写最终路径，失败会丢失上一份有效分享包并留下半成品。
- 修复：导入与导出均在缓存根中使用每次唯一的安全临时目录；ZIP 先写入目标同目录唯一临时文件，完整关闭、刷新并 `fsync` 后原子替换。所有失败路径精确清理本次临时归档与 staging。
- 验证：模拟 ZIP 成员写入中断，旧 `.gmshare` 逐字节保留，目标和缓存目录无临时残留；回归正常导出、导入去重、脱敏与完整性校验，完整 Windows 单元测试 `463 passed`。

### REL-041 · ebpack 校验与恢复工作目录隔离

- 状态：已完成
- 来源：ebpack 校验/恢复连续重入与并发审计。
- 风险：验包缓存目录由包文件名固定派生，同名包并发校验会互删已解压数据。恢复的解压、最终 staging 和旧数据保留目录也使用三个固定名称，开始时会删除同名目录，可破坏重入任务或用户在目标中已有的同名资料。
- 修复：验包在缓存根中使用每次唯一目录；恢复的解压、最终 staging 和旧数据保留目录均在目标根内使用每次唯一名称。清理阶段仅删除当前流程持有的精确路径。
- 验证：在缓存与恢复目标中预置旧固定名目录及哨兵文件，完成校验/恢复后哨兵逐字节保留且新唯一目录无残留；回归 ebpack 导出、完整性和恢复，完整 Windows 单元测试 `465 passed`。

### REL-042 · 外部工作区文本实际限量读取

- 状态：已完成
- 来源：外部编辑工作区连续 TOCTOU 与冗余读取审计。
- 风险：工作区 manifest、题目 metadata 和 Markdown 虽先通过 `stat` 检查 2/4/16 MiB 上限，随后却使用无界 `Path.read_text`；共享目录中的另一进程可在检查后替换或增长文件，绕过实际读取预算。该逻辑也与归档元数据限量读取重复。
- 修复：工作区三类文本统一复用归档基础层的普通文件限量读取器，以二进制限量读取后核对 `stat` 声明大小，并统一拒绝符号链接、缺失文件、超限与读取期间变化；随后再严格解码 UTF-8。
- 验证：直接禁用 `Path.read_text` 后，工作区受限读取仍能正确返回 UTF-8 内容；回归工作区导出、导入、冲突、类型化内容与路径穿越防护，完整 Windows 单元测试 `466 passed`。

### REL-043 · 章节模板导入预算与结构校验

- 状态：已完成
- 来源：剩余外部 JSON 输入面连续审计。
- 风险：章节模板导入直接使用无界 `read_text` 与 `json.loads`，没有文件大小、符号链接、根类型或章节数量预算；异常模板可在进入数据库事务前占用大量内存/CPU，非对象根也会泄漏底层异常。
- 修复：模板复用普通文件限量读取器，限制为 4 MiB，统一拒绝符号链接、读取期间变化和非 UTF-8/JSON；根节点必须为对象，`subject` 必须包含非空名称，`chapters` 必须为最多 10000 项的数组，v1/v2 导入均复用验证后的同一列表。
- 验证：将读取上限收紧到 4 字节后，5 字节输入在 JSON 解析前以超限错误拒绝；回归章节模板往返和同名不同路径的 v2 目录结构，完整 Windows 单元测试 `467 passed`。

### REL-044 · LocalFolder 元数据与锁文件限量读取

- 状态：已完成
- 来源：剩余无界 `read_text` 调用连续审计。
- 风险：LocalFolder 仓库、Release、设备和同步 manifest 虽先检查 8 MiB 大小，但随后仍无界读取，共享目录中的变更可绕过实际读取预算。锁文件完全无大小上限，异常大锁可在每次获取/释放时重复占用内存。
- 修复：所有 LocalFolder JSON 元数据复用普通文件限量读取器，保持 8 MiB 上限并增加实际限量读取、符号链接和读取前后大小校验；同步锁使用相同边界并单独限制为 64 KiB，非法锁继续按可恢复过期语义处理。
- 验证：禁用 `Path.read_text` 后仍可正确读取元数据 JSON；回归 LocalFolder 元数据大小错误语义、锁过期/释放、云备份和 Operation 同步，完整 Windows 单元测试 `468 passed`。

### REL-045 · GitHub Operation 批次流式校验

- 状态：已完成（2026-08-02）
- 风险：远端批次原先先整文件计算哈希，再以 `read_text().splitlines()` 重新整文件读取和拆行；大批次会产生重复 I/O 与多份内存驻留，而且记录数限制要到分配完成后才生效。
- 实现：下载后以单次二进制流逐行同步计算 SHA-256、解码和解析；限制单行 48 MiB、整批 64 MiB、物理行 100000，只有哈希匹配后才发布已解析 Operation，避免受篡改内容进入合并输入。
- 验证：覆盖哈希正确但单行超限、内容篡改、正常 GitHub 推拉及资料隔离；完整 Windows 单元测试 `468 passed`。

### REL-046 · GitHub Operation 待推批次预算

- 状态：已完成（2026-08-02）
- 风险：GitHub 待推批次原先会先把所有未推 Operation 全量加载，再通过 `join` 构造完整 JSONL 字符串并二次编码写盘；积压数据可造成重复内存驻留，且发送方可能上传接收方必然拒绝的超限批次。
- 实现：GitHub 查询最多取预算上限加一条并在加锁前拒绝超额记录；JSONL 改为逐条编码、即时校验 48 MiB 单行与 64 MiB 整批预算、同步写盘并计算 SHA-256，移除整批字符串和写后重读哈希。
- 验证：覆盖超限待推载荷在创建 Release 前失败且不产生远端副作用，并回归正常推拉、上传失败保留未推状态；完整 Windows 单元测试 `469 passed`。

### REL-047 · GitHub Operation 跨批累计预算

- 状态：已完成（2026-08-02）
- 风险：接收端虽然限制单批文件和行数，但会遍历最多 10000 个远端批次并把所有解析结果累计到同一列表；大量各自合规的历史批次仍可绕过单批预算并耗尽内存。
- 实现：跨批累计下载体积限制为 256 MiB、累计物理行限制为 250000；文件下载后先计入全局体积预算，每读取一条物理行即计入全局行预算，超限时在继续解析和合并前失败。
- 验证：覆盖单批合规但超过跨批累计体积预算的拒绝路径，并回归哈希、单行、正常推拉及资料隔离；完整 Windows 单元测试 `469 passed`。

### REL-048 · LocalFolder Operation 原子流式追加

- 状态：已完成（2026-08-02）
- 风险：本地文件夹同步追加 Operation 时先保留全部编码行，再通过 `b"".join` 复制为完整批次；接近 64 MiB 上限时产生约双倍字节内存峰值，且文件大小检查与真正打开追加之间存在不必要的状态间隔。
- 实现：打开追加文件后以实际文件尾为回滚点，逐条编码、即时校验单行和累计文件预算并完整写入；任意后续条目校验或 I/O 失败都会截断到原文件尾，新建文件失败时直接移除空残留，成功后统一 `fsync`。
- 验证：覆盖第二条 Operation 才超限时首条已流式写入但整批仍完整回滚，并回归文件预算、非法 UTF-8 与同步推拉；完整 Windows 单元测试 `470 passed`。

### REL-049 · LocalFolder Operation 跨设备读取预算

- 状态：已完成（2026-08-02）
- 风险：LocalFolder 拉取只限制单设备 `ops.jsonl` 为 64 MiB；最多 10000 个设备目录可分别满足限制，但累计读取与解析结果仍可能造成无界资源消耗。
- 实现：跨设备 Operation 日志声明体积和实际读取体积均限制为 256 MiB；每个文件在流式读取中再次累计实际字节并复核 64 MiB 单文件上限，覆盖检查后增长或替换场景。
- 验证：覆盖两个各自合规的设备日志在累计超过全局预算时拒绝，并回归单文件超限、物理行预算、非法 UTF-8 与同步推拉；完整 Windows 单元测试 `471 passed`。

### REL-050 · Operation ID 不可变性与定向幂等查询

- 状态：已完成（2026-08-02）
- 风险：同步拉取原先每次扫描并实例化全部已应用 Operation 历史；同时只按数据库历史跳过 ID，单次远端输入内相同 ID 的重复记录会被重复合并，相同 ID 的不同载荷也会静默进入同一实体批次。
- 实现：先校验并按 Operation ID 去重，完全相同的重复载荷只处理一次、相同 ID 的不同内容直接拒绝；幂等检查只针对本次有效 ID，以每组 500 个主键的查询获取已应用集合，不再加载无关历史行及载荷。
- 验证：覆盖两个设备提供相同 Operation 只应用一次，第三设备复用同一 ID 注入不同内容时拒绝且题目保持不变；完整 Windows 单元测试 `472 passed`。

### REL-051 · 已应用 Operation 历史载荷一致性

- 状态：已完成（2026-08-02）
- 风险：REL-050 已拒绝同次拉取中相同 ID 的不同载荷，但远端仅提供一条篡改记录、其 ID 又已存在于本地历史时，旧幂等路径仍会只按 ID 静默跳过，无法发现跨次拉取的 ID 复用。
- 实现：定向幂等查询同时读取匹配 ID 的历史载荷，重新解析并校验 Operation；远端规范化载荷必须与本地已应用载荷完全一致，否则拒绝合并，本地历史自身损坏也转换为明确领域错误。
- 验证：先应用合法 Operation，再从只有篡改载荷的新远端来源复用其 ID，确认拉取拒绝且题目内容保持不变；完整 Windows 单元测试 `472 passed`。

### REL-052 · Operation 设备来源绑定

- 状态：已完成（2026-08-02）
- 风险：GitHub 批次和 LocalFolder 设备目录虽然声明来源设备，但内部 Operation 的 `device_id` 原先无需一致；远端可把本机或第三方设备的操作放入另一设备容器，绕过来源排除并制造错误归属。GitHub 索引也未保存可核对的记录数。
- 实现：GitHub 索引写入 `operation_count`，接收端校验其类型、范围和物理行数，并拒绝协议 Operation 与批次 `device_id` 不一致；LocalFolder 在追加和读取两侧绑定 Operation `device_id` 与设备目录名，流式追加中途发现不一致仍完整回滚。
- 验证：覆盖哈希正确但内部设备被伪造的 GitHub 批次，以及写入错误设备目录时无日志残留；回归旧索引兼容、正常推拉、哈希与容量预算，完整 Windows 单元测试 `473 passed`。

### REL-053 · GitHub 未入索引批次补偿清理

- 状态：已完成（2026-08-02）
- 风险：GitHub Operation 流程先创建并上传 Release，最后才更新同步索引；上传、回读校验或索引写入失败时，远端会遗留永远不可达的批次 Release，重复失败可持续占用仓库配额。
- 实现：Release 创建后的上传、回读、哈希校验和索引提交均纳入补偿边界，失败时调用提供方删除未入索引的 tag；索引批次数组先复制再追加，写入失败不会修改提供方返回的共享对象，补偿删除失败会给出独立错误。
- 验证：覆盖上传失败和完整上传后索引写入失败，两种情况均删除已创建 Release、保持索引不变并保留本地未推 Operation；完整 Windows 单元测试 `474 passed`。

### REL-054 · GitHub Operation 索引发布前预检

- 状态：已完成（2026-08-02）
- 风险：批次索引结构原先在 Release 上传并回读后才验证，且发送端未阻止索引超过接收端 10000 批上限；畸形或已满索引仍会先制造远端 Release，随后依赖补偿删除，短暂产生副作用并增加失败面。
- 实现：JSONL 完成本地预算校验后、创建 Release 前读取并验证索引必须为对象、批次数组类型正确且尚有容量；在同步锁保护的同一流程中复用该索引快照提交新批次，不再上传后重新读取。
- 验证：覆盖畸形批次数组和达到批次数上限两种情况，均在 `create_release` 前失败且无远端创建记录；完整 Windows 单元测试 `474 passed`。

### REL-055 · Operation 标识边界与确定性时序

- 状态：已完成（2026-08-02）
- 风险：Operation 的主键、设备、资料库和实体 ID 原先没有对齐数据库 64 字符列，时间戳也只要求非空；GitHub 返回的跨批顺序会直接影响同字段连续更新的最终值，与 LocalFolder 的时间排序行为不一致。
- 实现：所有持久化 ID 限制为 64 字符，时间戳限制为 64 字符、必须是带时区的 ISO-8601，并规范化为 UTC；有效远端 Operation 在按实体分组前以规范化时间戳、目标 revision 和 ID 稳定排序。
- 验证：覆盖超长 ID、无时区及非法时间戳拒绝、时区偏移规范化，并模拟提供方倒序返回两次同字段更新，确认最终应用较新载荷；完整 Windows 单元测试 `475 passed`。

### REL-056 · Operation 变更字段预算与白名单

- 状态：已完成（2026-08-02）
- 风险：`changed_fields` 与 `base_fields` 原先只验证为对象，任意数量、任意名称的键都可进入三路合并、冲突快照和历史载荷；未知身份或关系字段虽最终不会覆写模型，却会放大排序与持久化成本并污染审计内容。
- 实现：两个字段映射均限制为最多 64 项，字段名必须为非空且不超过 64 字符；problem Operation 仅保留明确支持的题目字段以及 `revision`、`tags`，未知字段在协议规范化阶段过滤，同时继续应用同一 Operation 内的合法字段。
- 验证：覆盖超量字段、非字符串字段名拒绝、合法与未知字段混合时的过滤结果，并回归远端尝试覆写 ID、关系和更新时间时合法标题仍正常同步；完整 Windows 单元测试 `475 passed`。

### REL-057 · Operation 题目字段值前置规范化

- 状态：已完成（2026-08-02）
- 风险：题目字段值原先到实际合并写库时才转换；一条 priority 对象、非法布尔值或错误日期即可中断整个远端拉取，使同批其他合法 Operation 无法应用。标签数量和名称也没有在进入集合合并前受限。
- 实现：本地记录、远端输入和已应用历史统一经过题目字段规范化；revision、tags、日期、状态、数值、布尔和文本在存储/分组前校验，日期保持规范 UTC 字符串以便 JSON 持久化，创建阶段的 SQLAlchemy 未落默认值按模型默认语义补齐。
- 验证：同一设备日志先放入 priority 类型错误的 Operation、再放入合法解析更新，确认无效记录不入历史且合法记录继续应用；同时回归本地创建、复习日期、远端创建与附件同步，完整 Windows 单元测试 `476 passed`。

### REL-058 · Operation 标量与附件元数据边界

- 状态：已完成（2026-08-02）
- 风险：短文本字段、revision、难度/掌握度、复习次数及附件尺寸元数据原先可携带超出模型列宽或 SQLite 整数范围的值；载荷本身即使低于批次预算，仍可在落库阶段溢出并中断拉取。
- 实现：题目短文本按模型的 32/64/256 字符列宽校验，difficulty/mastery 限制为 1–5，revision、review_count 约束在非负 64 位整数范围；附件 ID、MIME、声明大小及宽高分别对齐 64、128、32 MiB 和 100000 像素上限。
- 验证：覆盖超长标题在同批中被隔离、超范围 revision，以及附件超长 ID/MIME、巨大尺寸与声明大小拒绝；完整 Windows 单元测试 `477 passed`。

### REL-059 · Operation taxonomy 外键前置检查

- 状态：已完成（2026-08-02）
- 风险：合法长度但本地不存在的 `subject_id` / `chapter_id` 原先会通过协议校验并进入合并，直到事务提交时触发外键失败；单条引用尚未同步 taxonomy 的 Operation 可阻断同批所有合法题目更新。
- 实现：有效 Operation 去重后汇总本次变更引用的科目与章节 ID，以每组 500 个主键定向查询本地 taxonomy；缺少目标的 Operation 不写入已应用历史并延后到后续拉取，空关系字符串规范化为 `None`，其余合法 Operation 继续合并。
- 验证：同一日志包含不存在章节引用与合法解析更新时，确认前者保持待重试且不入历史，后者正常应用；完整 Windows 单元测试 `477 passed`。

### REL-060 · Operation 附件 ID 不可变性

- 状态：已完成（2026-08-02）
- 风险：单个 Operation 可重复声明同一附件 ID，多个同实体 Operation 也可为同一 ID 提供不同哈希与内容；聚合器原先使用字典后写覆盖前写，最终落盘载荷取决于批次顺序。
- 实现：协议校验拒绝单个 Operation 内重复 attachment ID；实体合并批次聚合附件时允许完全相同的重复声明，但同一 ID 的任意元数据或内容差异都会在解码、写文件和建 Asset 前拒绝。
- 验证：覆盖单 Operation 重复 ID，以及两个 Operation 为同一 ID 提供不同内容时的冲突路径，确认不会创建资产记录；完整 Windows 单元测试 `478 passed`。

### REL-061 · 实体 Operation 附件累计预算

- 状态：已完成（2026-08-02）
- 风险：每个 Operation 的附件虽限制为 32 MiB，但同一题目的多个 Operation 会在一次实体合并中聚合；多个各自合规的附件可叠加解码和落盘，绕过单 Operation 预算并产生部分对象副作用。
- 实现：汇总实体批次引用的派生题图后，先按声明 `size_bytes` 核对 32 MiB 总预算，全部通过才开始 Base64 解码；解码过程中再以实际字节复核同一总预算，超限发生在任何对象写入前。
- 验证：两个 Operation 分别携带 3 字节附件、将测试总预算收紧至 4 字节时，确认预检拒绝且数据库和对象库均无资产残留；完整 Windows 单元测试 `479 passed`。

### REL-062 · Operation 附件哈希全量预检

- 状态：已完成（2026-08-02）
- 风险：协议校验原先虽然解码附件并核对大小，却不核对 SHA-256；实体物化又按附件逐个“校验后立即写盘”，后续附件哈希错误时，前序对象文件已经成为无数据库引用的残留。
- 实现：`validate_operation` 在协议入口核对每个附件实际内容哈希；实体合并先解码、累计预算并校验全部引用附件，将最多 32 MiB 的已验证内容暂存内存，全部通过后才进入对象库与 Asset 建库阶段。
- 验证：覆盖单 Operation 声明错误哈希的拒绝，以及排序靠后的第二个附件哈希错误时，确认排序靠前的合法附件也未写入数据库或对象库；完整 Windows 单元测试 `480 passed`。

### REL-063 · 统一笔记搜索索引分批重建

- 状态：已完成（2026-08-02）
- 风险：笔记统一搜索索引重建原先一次性加载全部 NoteDocument 及其块、标签、集合，并同时构造完整投影列表；笔记正文增长时 ORM 对象和投影文本会共同常驻，写入前产生无界内存峰值。
- 实现：关联预取查询使用 `yield_per` 与 `partitions` 按 200 条读取，每批立即构造投影并分别写入普通索引和 FTS，完成后释放批次列表；重建仍处于原事务，保留原子替换语义和自动钩子行为。
- 验证：将批大小收紧为 2 后重建 5 篇笔记，确认写入批次严格为 `2/2/1`、计数和搜索结果完整；完整 Windows 单元测试 `481 passed`。

### REL-064 · 错题搜索索引分批重建

- 状态：已完成（2026-08-02）
- 风险：主错题搜索索引全量重建原先一次性加载所有 `Problem` 及标签并同时构造完整投影列表；题目正文增长时 ORM 对象和普通/FTS 写入参数共同形成无界内存峰值。
- 实现：科目与章节路径只计算一次，题目与标签按 200 条 `yield_per` / `partitions` 分区读取；每批使用共享投影构造器立即写入普通索引和 FTS，定向 upsert 与全量重建保持一致语义和原子事务。
- 验证：将批大小收紧为 2 后重建 3 道题，确认写入批次严格为 `2/1` 且投影计数完整；完整 Windows 单元测试 `482 passed`。

### REL-065 · 分享包流式导出与输出预算

- 状态：已完成（2026-08-02）
- 风险：`.gmshare` 导出会一次性加载全部题目及关联，再同时保留完整序列化记录列表；导出端也未执行导入端已有的题目数、资源引用数和单条 JSONL 大小限制，可能产生高内存峰值以及应用自身无法重新导入的超限包。
- 实现：题目与关联改为每批 200 条 `yield_per` / `partitions` 读取，每条完成脱敏、资源复制和预算校验后立即写入 JSONL；题目、资源引用和 UTF-8 单行大小在产包阶段即按协议上限拒绝，失败仍只清理唯一 staging，不触碰既有目标文件。
- 验证：批大小收紧为 2 后导出 3 道题，确认读取批次严格为 `2/1` 且 JSONL 记录完整；将题目预算收紧为 2 后确认第三题立即失败、目标和缓存均无残留；完整 Windows 单元测试 `484 passed`。

### REL-066 · 用户可见文本导出原子写入

- 状态：已完成（2026-08-02）
- 风险：章节模板、复习记录 CSV、复习汇总和题目 AI 对话 Markdown 原先直接打开最终路径写入；磁盘、编码或序列化中断会先截断用户已有导出。复习 CSV 还会先加载整场 `StudyRecord` 列表，长会话形成无界内存峰值。
- 实现：新增同目录唯一临时文件文本写入器，完成写入后统一 `flush`、`fsync` 和 `os.replace`，异常时只清理临时文件；四类导出均接入该边界，CSV 保留 UTF-8 BOM 并改为每批 200 条读取和写出。
- 验证：模拟 CSV 写入器在表头阶段失败，确认既有目标逐字节保留且临时文件无残留；批大小收紧为 2 后导出 3 条记录，确认读取批次严格为 `2/1`、表头与记录完整；相关 39 项定向测试及完整 Windows 单元测试 `486 passed`。

### REL-067 · AI 流式响应与结构化 JSON 解析边界

- 状态：已完成（2026-08-02）
- 风险：SSE 响应原先通过迭代器获取整条物理行，远端持续发送无换行数据时可能在累计预算检查前形成无界缓冲；结构化输出使用首个 `{` 到最后一个 `}` 的贪婪截取，前置说明花括号或多个对象会让后续合法结果无法解析，过深 JSON 还会泄漏底层递归异常。
- 实现：SSE 改为按剩余总预算调用限长 `readline`，最多只读取 `limit + 1` 字节后立即拒绝；嵌入 JSON 改为最多 128 个起始候选的 `JSONDecoder.raw_decode` 增量解析，并统一将解码、Unicode 与递归深度错误转换为可处理的领域错误。
- 验证：覆盖无换行超限流的首次读取上限、错误花括号后合法对象、多对象选择、候选数量预算和深层嵌套错误；相关 23 项定向测试及完整 Windows 单元测试 `491 passed`。

### REL-068 · CloudBase 正式远端收敛

- 状态：已完成（2026-08-02）
- 变更：移除 Git 仓库型云端适配器、工厂分支、配置段、设置入口、Android 令牌字段、探测工具和专属测试；默认正式远端改为 CloudBase，LocalFolder 仅保留离线测试。旧 Windows 偏好自动迁移到 `cloudbase`，Android 初始化时清除旧令牌槽。
- 完善：原不可变 Release Operation 批次实现改为 provider 无关命名并由 CloudBase 的原子锁、私有附件、回读哈希和原子 manifest 接管；发布或索引失败继续删除未入索引 Release 并保留本地未推记录。
- 文档：同步任务书、CloudBase 网关契约、快照/Operation 协议、Windows/Android 说明和发布矩阵统一为 CloudBase 正式远端，不再宣称旧 provider 可用。
- 验证：配置、CloudBase、同步、设置和快照定向测试 `100 passed`；同步发布矩阵 `81 passed`；完整 Windows 单元测试 `477 passed`，Ruff、Python 编译和差异检查通过。测试总数因删除两套已退役 provider 专属测试而下降。

### REL-069 · Windows 启动闪窗回归修复

- 状态：已完成（2026-08-02）
- 风险：笔记详情和复习会话在尚未挂入主窗口时主动显示动态说明标签；空复习队列及录题内容块也在构造期请求可见，可能让 Windows Qt 短暂暴露未稳定归属的灰色窗口。
- 实现：动态说明改为随 `PageHeader` 初始构造并由父页面统一显示；默认可见控件不再调用 `show()`/`setVisible(True)`，只对确需默认隐藏的内容调用 `hide()`。
- 验证：新增笔记页与复习页构造期主动显示回归；相关 UI 测试 `54 passed`，无界面冷启动诊断确认四处源码请求消失，完整 Windows 单元测试 `479 passed`。

### REL-070 · UI-203 万级列表性能证据

- 状态：已完成（2026-08-02）
- 工具：`performance_baseline.py --ui203 --notes 10000` 生成混合笔记和同规模复习草稿队列，测量笔记页、复习构建器、滚动、进程工作集与 QWidget 数量；少于 10000 篇时拒绝运行，资料继续使用临时隔离目录并自动清理。
- 结果：三轮中位数为笔记刷新 `653.818ms`、复习构建器刷新 `2900.148ms`，滚动往返仅 `0.545ms`、`1.414ms`/`0.465ms`；QWidget 增量固定为 178/65，证明 UI-203 不应实施富内容 QWidget 虚拟化。
- 后续：将全量详情关系加载和来源/草稿双列表重复填充登记为就绪任务 UI-204；原始 JSON 报告留在系统临时目录，不进入仓库。专项工具测试 `7 passed`，完整 Windows 单元测试 `482 passed`。

### REL-071 · 全局下拉视觉规范回归

- 状态：已完成（2026-08-02）
- 变更：全局主题统一 `QComboBox` 与 `QMenu` 的悬浮面板边框、圆角、8px 面板留白、38px 条目高度、横向内边距、内缩分隔线、展开态及禁用态；悬停和选中沿用主题表面色，不再以高饱和主色覆盖文字。
- 验证：明暗主题均由同一组语义令牌生成；新增样式结构回归并完成暗色离屏渲染检查，未引入页面内联样式或改变选择、键盘导航及菜单动作语义；完整 Windows 单元测试 `484 passed`。

### REL-072 · 组合框原生弹层透明化回归

- 状态：已纠正（2026-08-03）
- 纠正原因：Windows 实机未稳定支持该 Qt 私有弹层的透明合成，部分弹层仍保留方壳，唯一生效的弹层则显示黑色透明区；同时 QSS 条目圆角会被原生委托覆盖。
- 变更：撤销透明背景方案；`QComboBoxPrivateContainer` 的窗口可见区域改为仅包含其内部列表视图，排除上下原生预留区，而非把整个更大的容器裁成第二个圆角背景。为每个项目视图安装共享 `SoftItemDelegate`，覆盖所有直接创建的组合框，而非只处理个别页面。
- 验证：专项测试覆盖真实组合框展开后的容器可见区域与列表几何严格一致、背景属性、边框和委托；离屏截图确认只显示单个列表面板、无黑底且选中项内缩圆角，不改变弹层数据、焦点、键盘导航或动作触发逻辑；完整 Windows 单元测试 `485 passed`。

### REL-073 · AI 模型选择入口可发现性

- 状态：已完成（2026-08-03）
- 变更：新增可复用 `ChevronComboBox`，在保留可编辑语义的同时绘制主题化向下箭头；AI 图片模型选择改用该控件，获取动作更名为“获取可用模型名称”，异步完成后的按钮恢复文案同步更新。
- 验证：设置页专项与主题回归 `70 passed`，并完成浅色离屏渲染检查；模型获取、手动输入、当前值保存及下拉选择逻辑未改变；完整 Windows 单元测试 `487 passed`。

### REL-074 · AI 录题裁剪字段滚轮隔离

- 状态：已完成（2026-08-03）
- 变更：新增整数与小数两类滚轮安全数字控件，AI 内容块的来源图片序号及四个归一化裁剪字段统一采用该控件；其 `wheelEvent` 只忽略事件，不调用数字控件默认增减逻辑。
- 验证：正负滚轮专项覆盖五个字段，确认字段值、内容块和 `changed` 信号均保持不变；键盘及步进按钮的既有写回链路未修改，相关 UI 专项测试 `76 passed`，完整 Windows 单元测试 `487 passed`。

### REL-075 · CloudBase 发布附件不可变性

- 状态：已完成（2026-08-03）
- 风险：网关原先把同一发布标签下的同名附件冲突处理为更新，并复用由发布标签组成的对象路径；重复或并发上传可能在 manifest 提交后替换既有附件，破坏不可变批次和恢复哈希语义。对象路径还直接包含身份服务返回的原始 subject。
- 修复：签发上传地址前拒绝既有附件；每个上传会话使用独立 `upload_id` 对象路径，提交时在事务内以 `DELETE ... RETURNING` 原子认领会话并只允许普通 INSERT。并发失败会回滚保留会话，随后由过期清理删除孤儿对象；用户对象目录改用 subject 的 SHA-256，不暴露原始身份标识。
- 验证：Node 回归锁定同名附件拒绝、无冲突更新、独立上传路径、原子会话认领和稳定匿名命名空间。

### REL-076 · CloudBase 多实例配额原子性

- 状态：已完成（2026-08-03）
- 风险：资料库数量与云存储预算原先采用无锁的“读取后写入”；并发请求或多个函数实例可同时通过检查。存储统计还忽略已签发但尚未提交的上传会话，用户可批量预留对象绕过预算。
- 修复：两类配额均在 PostgreSQL 事务内按可信 subject 获取 advisory lock 后检查和写入；存储预算合并已提交附件与所有未过期上传会话。重复打开既有资料库不再错误消耗新名额，也不会在达到上限后被拒绝。
- 验证：Node 回归锁定多实例共享锁、上传预留计费、检查与写入同一事务及既有资料库幂等语义。

### REL-077 · CloudBase 上传会话原子认领与可重试清理

- 状态：已完成（2026-08-03）
- 风险：同一个一次性上传地址的并发 PUT 可同时通过未上传检查并写入同一路径；过期清理又先删除会话记录再静默忽略对象删除失败，可能产生内容竞态和无法追踪的永久孤儿对象。
- 修复：上传 PUT 先以条件 UPDATE 原子设置 `claimed_at`，同一会话只允许一个执行者；上传失败释放认领，成功时同步清除。过期清理排除活跃认领，并仅在对象删除成功后删除会话，失败记录留待下次重试。初始化 SQL 以幂等迁移补充认领时间列。
- 验证：Node 回归锁定单执行者认领、失败重试、成功终结以及对象删除失败时保留索引。

### REL-078 · CloudBase 分布式限流

- 状态：已完成（2026-08-03）
- 风险：原限流使用 Node 进程内 Map；函数横向扩容会按实例重复发放额度，持续出现的新用户键也不会回收。
- 修复：登录验证后的请求改由 PostgreSQL `rate_limits` 单行 UPSERT 原子累加，同一 subject 在所有函数实例共享分钟窗口；数据库仅保留每位已见用户的一行状态，移除进程内身份 Map。初始化 SQL 幂等创建表并启用 RLS。
- 验证：Node 回归锁定共享原子计数、异步执行和无进程内无限身份缓存。

### REL-079 · CloudBase 服务端强制写入租约

- 状态：已完成（2026-08-03）
- 风险：客户端会在备份和增量同步前主动获取主写入锁，但网关的清单、发布和附件写接口并未验证锁；旧版、篡改或异常客户端可绕过协调直接并发改写远端状态。
- 修复：CloudBase provider 只在成功持有锁时发送写请求，并附带设备 ID；网关对清单写入、发布创建/删除、附件签发/提交在同一事务内校验有效租约并续期 15 分钟。资料别名写入补齐获取和释放锁流程。
- 验证：Python 回归覆盖无锁拒绝、设备 ID 透传与释放后拒绝；Node 回归逐项锁定五类远端写操作的服务端租约校验。

### REL-080 · CloudBase 对象删除持久重试

- 状态：已完成（2026-08-03）
- 风险：Release 删除原先先移除数据库元数据，再调用 Cloud Storage 删除对象并静默吞掉异常；临时存储故障会留下失去索引、永久占用免费额度的孤儿对象。
- 修复：在删除 Release 的同一事务中先把全部 `file_id` 写入持久化删除队列，再删除元数据并提交；提交后逐对象清理，失败则累计尝试次数并保留记录，后续 Release 列表或删除动作继续重试。初始化 SQL 幂等创建队列表、索引和 RLS。
- 验证：Node 回归锁定“入队早于元数据删除及提交”的顺序，并确认对象删除失败只更新尝试状态、不丢失队列记录；网关测试 `18 passed`，完整 Windows 单元测试 `499 passed`，Ruff 与差异检查通过。

### REL-081 · CloudBase 数值配置失效保护

- 状态：已完成（2026-08-03）
- 风险：网关对端口、对象上限、用户预算、资料库数量、限流和连接池使用裸 `Number()`；误填 `NaN`、小数或无穷值时比较可能恒为 false，静默绕过对象大小与费用保护。
- 修复：所有数值环境变量统一经安全整数解析器校验最小值和硬上限；空值采用默认值，非法配置在进程启动阶段明确失败。`MAX_ASSET_BYTES` 继续硬限制为 512 MiB，连接池、资料库和限流也设置合理上界。
- 验证：Node 单元测试覆盖默认值、边界值、`NaN`、小数、零、越界和无穷值拒绝。

### REL-082 · CloudBase 身份边界与损坏会话保护

- 状态：已完成（2026-08-03）
- 风险：网关环境 ID 未限制字符和长度，身份响应整包解析无大小边界；系统凭据中的畸形 `expires_at` 或身份服务返回的非法 `expires_in` 会泄漏底层转换异常并中断登录流程。
- 修复：桌面端和网关统一只接受 1–64 位字母、数字及连字符的环境 ID，身份服务响应以流式累计方式限制为 64 KiB，subject 限制为 512 字节。损坏的结构化凭据不再回退成旧版裸 token，非法服务端有效期转为领域错误，且本地有效期最多信任 7 天。
- 验证：Python/Node 回归覆盖官方环境 ID、URL/点号/超长注入拒绝、损坏凭据与非法有效期响应；认证与 provider 定向测试 `15 passed`，网关测试 `20 passed`，Ruff 与差异检查通过。

### REL-083 · CloudBase 写入租约任务隔离

- 状态：已完成（2026-08-03）
- 风险：写锁只以设备 ID 标识，并允许同一设备重复获取；同设备并发备份/同步会共享锁，其中一个任务提前释放后，另一个任务仍在运行却失去保护。
- 修复：CloudBase provider 每次加锁生成独立随机 `lease_id` 并仅保存在内存；数据库锁行同时保存设备与租约。续租、所有远端写入和释放必须匹配二者；同设备不同租约在旧租约到期前也无法获取。初始化 SQL 以幂等迁移补充租约列。
- 验证：Python 回归确认写入和释放透传同一随机租约；Node 回归锁定同设备不同任务不复用租约、释放必须三键匹配。

### REL-084 · CloudBase 远端元数据预算

- 状态：已完成（2026-08-03）
- 风险：Release 和单 Release 附件数量没有服务端上限，列表还会读取资料库的全部附件；恶意或异常客户端可用小对象持续堆积数据库，并让普通列表响应无界膨胀。
- 修复：单资料库 Release 默认上限 10000，单 Release 已提交附件与有效上传会话合计默认上限 16；检查与创建继续受写入租约事务串行保护。Release 说明限制为 64 KiB，列表固定最近 100 条并只连接该页标签的附件；历史超限说明返回空体而非放大响应。
- 验证：Node 回归锁定两级元数据配额、有效会话计数、说明体边界以及列表附件查询的当前页约束。

## 每次交付门禁

1. 改动范围 Ruff 和 Python 编译检查通过。
2. 对应单元测试通过；发布节点运行完整测试。
3. 数据迁移和同步改动必须包含失败恢复测试。
4. 临时资料、凭据、截图和性能数据不得误入仓库。
5. 更新对应分任务书与 [`../完成记录.md`](../完成记录.md)，再提交。
