# 研错库完成记录

> 精简版，最后更新 2026-08-02。仅保留当前基线和近期交付；完整历史见 Git 提交记录与协议/Schema 变更文档。

## 当前基线

| 项目 | 状态 |
| --- | --- |
| 路线图 A-K | 已完成 |
| Windows 数据库 | `schema_version=22` |
| 数据语义 | `data_format_version=1` |
| Windows 单元测试 | `474 passed` |
| Android Gradle 测试 | 待完成本机依赖解析后验证 |

## 近期交付

### 2026-08-02 · REL-054 GitHub Operation 索引发布前预检

- GitHub Operation 索引的对象结构、批次数组和 10000 批容量上限改在创建 Release 前验证，已满或损坏索引不会产生远端副作用。
- 通过预检的索引快照在同一同步锁内直接用于提交，移除上传后的重复读取；完整单元测试 `474 passed`。

### 2026-08-02 · REL-053 GitHub 未入索引批次补偿清理

- GitHub Operation Release 创建后若上传、回读校验或索引提交失败，会补偿删除未入索引的远端 Release，避免不可达批次长期占用仓库配额。
- 索引在复制批次数组后修改，失败不污染提供方返回对象；本地 Operation 继续保持未推状态，完整单元测试 `474 passed`。

### 2026-08-02 · REL-052 Operation 设备来源绑定

- GitHub 批次把记录数写入同步索引并核对类型、上限和实际物理行数，内部协议 Operation 必须与批次声明的设备一致。
- LocalFolder 在写入和读取两侧绑定 Operation `device_id` 与设备目录，来源伪装会被拒绝且追加失败不留下日志；完整单元测试 `473 passed`。

### 2026-08-02 · REL-051 已应用 Operation 历史载荷一致性

- 幂等查询在匹配本次输入 ID 的同时读取并重新校验历史载荷，远端相同 ID 必须与已应用 Operation 内容完全一致。
- 跨次拉取复用已应用 ID 注入不同载荷不再被静默跳过，本地历史 JSON 或 Operation 结构损坏也会明确失败；完整单元测试 `472 passed`。

### 2026-08-02 · REL-050 Operation ID 不可变性与定向幂等查询

- 拉取先按 Operation ID 校验不可变性：相同载荷去重，不同载荷复用同一 ID 时拒绝，避免重复应用或远端来源歧义。
- 已应用检查改为只查询本次输入涉及的主键，并以 500 个 ID 分批执行，不再扫描和实例化全部同步历史；完整单元测试 `472 passed`。

### 2026-08-02 · REL-049 LocalFolder Operation 跨设备读取预算

- LocalFolder 拉取新增跨设备 256 MiB Operation 日志累计预算，多个单独合规的设备日志不能再绕过整体读取边界。
- 同时核对文件声明体积和流式读取的实际字节，单文件在读取期间增长也会触发 64 MiB 上限；完整单元测试 `471 passed`。

### 2026-08-02 · REL-048 LocalFolder Operation 原子流式追加

- LocalFolder Operation 追加移除逐行字节列表和整批 `join`，改为逐条受限编码并直接追加，降低接近 64 MiB 批次时的重复内存峰值。
- 追加中途发生后续条目超限或 I/O 失败时恢复原文件长度，新文件失败不留下空日志；完整单元测试 `470 passed`。

### 2026-08-02 · REL-047 GitHub Operation 跨批累计预算

- GitHub 拉取在单批 64 MiB / 100000 行限制外，新增跨批 256 MiB 与 250000 物理行的全局预算，历史批次不能再通过分别合规绕过内存边界。
- 累计体积在下载后、解析前检查，累计行数在逐行读取时即时检查；完整单元测试 `469 passed`。

### 2026-08-02 · REL-046 GitHub Operation 待推批次预算

- GitHub 待推 Operation 查询限制为单批上限加一条，超额时在获取远端锁前拒绝，避免无界积压一次性进入发送流程。
- JSONL 改为逐条受限写入并同步计算 SHA-256，单行 48 MiB、整批 64 MiB 和 100000 条预算与接收端一致；完整单元测试 `469 passed`。

### 2026-08-02 · REL-045 GitHub Operation 批次流式校验

- GitHub 远端 Operation 批次改为单次受限二进制流完成 SHA-256、UTF-8 解码与 JSON 解析，不再重复整文件读取或调用 `splitlines()` 复制内容。
- 单行、整批和物理行数预算在读取过程中即时生效，且只有哈希匹配后才返回解析结果；完整单元测试 `468 passed`。

### 2026-08-02 · REL-044 LocalFolder 元数据与锁文件限量读取

- LocalFolder 仓库、Release、设备和同步 JSON 复用 8 MiB 受限文件读取器，不再在 `stat` 后无界读取。
- 同步锁文件单独限制为 64 KiB，并统一校验符号链接与读取前后大小；完整单元测试 `468 passed`。

### 2026-08-02 · REL-043 章节模板导入预算与结构校验

- 章节模板改用 4 MiB、符号链接及读取前后大小受限的通用文件读取器，非法 UTF-8/JSON 统一转换为领域错误。
- 根节点、科目名与章节数组在数据库处理前校验，单模板最多 10000 个章节；完整单元测试 `467 passed`。

### 2026-08-02 · REL-042 外部工作区文本实际限量读取

- 工作区 manifest、metadata 和 Markdown 统一复用受限普通文件读取器，二进制限量读取后再核对声明大小和 UTF-8。
- 检查后增长/替换、符号链接与超限文件不再能进入无界 `read_text`；完整单元测试 `466 passed`。

### 2026-08-02 · REL-041 ebpack 校验与恢复工作目录隔离

- ebpack 验包使用每次唯一缓存目录，恢复的解压、最终 staging 与旧数据保留目录也改为目标根内唯一名称。
- 清理仅作用于本次流程持有的路径，不再删除重入任务或用户旧同名目录；完整单元测试 `465 passed`。

### 2026-08-02 · REL-040 gmshare 原子导出与唯一工作目录

- gmshare 导入与导出改用每次唯一缓存目录，同秒导出或同名包导入不再互删中间数据。
- ZIP 在同目录临时文件中完整写入、`fsync` 后原子替换；失败保留旧分享包且无残留，完整单元测试 `463 passed`。

### 2026-08-02 · REL-039 gmshare 元数据与物理行预算

- gmshare manifest 与资源索引改用 8 MiB、符号链接及读取前后大小受限的通用归档读取器，ebpack 复用同一实现。
- checksum 和题目 JSONL 最多各处理 20000 个物理行，空白/注释行不再绕过预算；完整单元测试 `462 passed`。

### 2026-08-02 · REL-038 本地 ZIP 备份载荷完整性

- 本地备份 v2 在 manifest 中完整记录数据库、身份和资源载荷的 SHA-256；恢复在覆盖目标前核对载荷集合、哈希格式与实际内容。
- 继续兼容无 checksum 的 v1 备份；损坏资源不再静默进入恢复资料，完整单元测试 `460 passed`。

### 2026-08-02 · REL-037 备份身份文件语义一致性

- 新增不改写磁盘的身份读取验证；本地 ZIP 和 ebpack 导出拒绝与当前用户、设备或资料库不一致的盘上身份，并以运行时身份快照入包。
- 本地恢复与 ebpack 验包会交叉核对 identity 和 manifest，错位身份不会覆盖目标资料；完整单元测试 `459 passed`。

### 2026-08-02 · REL-036 本地备份恢复元数据与临时目录边界

- 本地备份 manifest 在 JSON 解析前按 1 MiB 预算限量读取，并校验 ZIP 声明/实际大小；ebpack 复用同一归档读取边界。
- 解压、最终 staging 和旧数据保留目录改为每次唯一名称，失败后精确清理；完整单元测试 `456 passed`。

### 2026-08-02 · REL-035 本地 ZIP 备份一致性与原子交付

- 本地 ZIP 备份与 ebpack 共用 SQLite Online Backup 快照边界，备份包会包含 WAL 中已提交更改，并拒绝身份源符号链接。
- ZIP 改为唯一 staging 与同目录临时文件写入，完整关闭、`fsync` 后原子替换；失败保留旧备份且无残留，完整单元测试 `455 passed`。

### 2026-08-02 · REL-034 ebpack SQLite WAL 一致性快照

- ebpack 数据库备份从直接复制主库文件改为 SQLite Online Backup API，会合并 WAL 中已提交页后生成一致性快照。
- 数据库与身份源文件拒绝符号链接；新增保持 WAL 写连接的回归用例，完整单元测试 `453 passed`。

### 2026-08-02 · REL-033 ebpack 导出原子交付

- ebpack 导出 staging 改为每次唯一目录，避免同秒并发导出相互删除中间数据。
- ZIP 先在目标同目录的唯一临时文件中完整写入并 `fsync`，再原子替换旧备份；失败时旧文件和目录均无残留；完整单元测试 `452 passed`。

### 2026-08-02 · REL-032 ebpack 元数据读取预算

- ebpack 的 manifest、checksum 表和资源索引统一限制为 8 MiB，ZIP 内成员在解压/解析前检查声明与实际读取量。
- 解压后元数据拒绝符号链接并核对读取前后大小，超限数据不再进入 JSON 解析或分行；完整单元测试 `451 passed`。

### 2026-08-02 · REL-031 视觉引用解码与请求预算

- 单条题目对话最多接受 20 个视觉引用，并在用户消息持久化前拒绝超量输入；重建历史快照时同样执行该边界。
- 框选原图在解码前校验 2500 万像素上限，裁剪 PNG 合计限制为 32 MiB，异常图片不会触发 AI 请求；完整单元测试 `449 passed`。

### 2026-08-02 · REL-030 本地 Operation 日志写入与流式读取

- `ops.jsonl` 追加前校验对象、行长、数量和写后 64 MiB 总量，完整写入并 `fsync`；中途失败按原长度截断回滚。
- 读取改为限长逐行解析并跨设备累计物理行预算，不再同时保留整文件和分割行列表；完整单元测试 `447 passed`。

### 2026-08-02 · REL-029 本地文件夹 Release 元数据原子化

- 仓库、Release 与 tombstone JSON 统一执行 8 MiB 预算、唯一临时文件、刷新、`fsync` 和原子替换，失败的 Release 不留下空目录。
- Release 列表拒绝符号链接附件并忽略上传中的临时文件；完整单元测试 `446 passed`。

### 2026-08-02 · REL-028 本地文件夹附件复制边界

- LocalFolder 上传和下载拒绝源/目标符号链接并限制为 512 MiB，避免共享目录借链接读取或覆盖边界外文件。
- 附件使用短名称唯一临时文件限量复制、刷新、`fsync` 后原子替换，兼顾失败清理和 Windows 深路径；完整单元测试 `445 passed`。

### 2026-08-02 · REL-027 用户偏好文件统一持久化边界

- AI、云端、主题和阅读缩放偏好统一使用 1 MiB、UTF-8、对象根及符号链接受限的读取器，移除四份重复 JSON 读写代码。
- 写入使用唯一临时文件、刷新、`fsync` 和原子替换；损坏旧文件不会再被静默清空覆盖；完整单元测试 `443 passed`。

### 2026-08-02 · REL-026 本地身份文件完整性

- `identity.json` 限制为 64 KiB，并校验符号链接、JSON 根类型、各类 ID、profile/snapshot 与显示字段，路径型身份不会进入同步流程。
- 身份创建、资料绑定和快照头更新改为刷新、`fsync` 后原子替换，失败路径清理临时文件；完整单元测试 `441 passed`。

### 2026-08-02 · REL-025 当日复习日期网络去重

- 当日复习日期的网络确认或本地降级结果缓存 5 分钟，批量加入题目不再逐题重复最长 2 秒的外部请求。
- 日期请求接入安全 HTTPS 重定向并覆盖网络与解析失败降级；完整单元测试 `438 passed`。

### 2026-08-02 · REL-024 AI API 初始连接凭据保护

- AI 模型列表、普通请求和流式请求均在读取密钥前强制验证 HTTPS Base URL，并拒绝内嵌凭据、查询参数和片段。
- HTTP 或可泄露凭据的地址不会到达网络层，不再只依赖设置页连接测试；完整单元测试 `437 passed`。

### 2026-08-02 · REL-023 Operation 附件累计预算校验

- 32 MiB Operation 附件预算成为生产端与接收端共享的领域常量，接收端按实际 Base64 解码字节累计并拒绝空附件或总量超限。
- 原有附件数量、类型、声明大小和哈希规则保持不变；完整单元测试 `434 passed`。

### 2026-08-02 · REL-022 增量同步题图读取预算前置

- Operation 派生题图在读取前先按 32 MiB 剩余预算检查文件大小，再限量读取并核对前后大小，不再完整加载超限对象后才失败。
- SHA-256、缺失引用与正常结构化题图同步语义保持不变；完整单元测试 `433 passed`。

### 2026-08-02 · REL-021 富题干题图渲染预算

- 题库、题目详情和复习页物化结构化题图时限制为最多 50 张、单张 16 MiB、原始数据合计 32 MiB，并核对读取前后大小。
- 超限或变化中的题图保留题注占位而不进入 Base64，正常小图渲染不变；完整单元测试 `432 passed`。

### 2026-08-02 · REL-020 题目对话原图上下文预算

- 题目对话授权附带原图时限制为最多 20 张、单张 32 MiB、原始数据合计 64 MiB，并核对读取前后大小。
- 超限或变化中的原图会让当前消息安全失败且不会调用 AI，无图文字降级与原图顺序保持不变；完整单元测试 `430 passed`。

### 2026-08-02 · REL-019 AI 图片请求内存预算

- 普通与流式视觉请求合并为同一受限图片编码流程：最多 20 张、单张 32 MiB、原始图片合计 64 MiB，并核对读取前后大小。
- 空文件、超限文件或读取期间变化会在 Base64 编码和网络请求前失败；完整单元测试 `429 passed`。

### 2026-08-02 · REL-018 云端大文件流式上传

- GitHub 与 CloudBase 上传备份包时改用 1 MiB 文件块迭代器并显式发送内容长度，不再先把最多 512 MiB 的文件完整读入内存。
- GitHub 每次网络重试重新打开文件流，避免复用已耗尽的请求体；完整单元测试 `428 passed`。

### 2026-08-02 · REL-017 GitLink 附件路径与传输预算

- GitLink 资源名在使用前完成路径字符校验，并移除曾按远端名称复制本地临时文件的冗余流程，路径型名称不再可能越出源目录。
- 公开 Release 响应限制为 8 MiB，附件上传与实际下载限制为 512 MiB，超限下载删除部分文件；完整单元测试 `426 passed`。

### 2026-08-02 · REL-016 HTTPS 重定向凭据隔离

- AI、GitHub 与 CloudBase 认证请求只跟随同源 HTTPS 重定向，并统一拒绝降级到 HTTP，默认重定向不再复制密钥到第三方源站。
- GitHub 附件下载所需的跨源 HTTPS 跳转会先移除 Authorization、Cookie、代理授权和 API Key 头；完整单元测试 `423 passed`。

### 2026-08-02 · REL-015 CloudBase 网关传输边界

- CloudBase 网关和签名存储地址统一强制 HTTPS 且禁止内嵌凭据；网关响应限制为 8 MiB，上传与下载对象限制为 512 MiB。
- 下载改为限量流式落盘，超限或网络失败会删除部分文件，并向业务层公布相同容量能力；完整单元测试 `419 passed`。

### 2026-08-02 · REL-014 GitHub 认证源站与下载预算

- 携带 GitHub Bearer 令牌的请求仅允许同 HTTPS API 源站及官方上传源站，远端返回的跨源 URL 不再可能带走凭据。
- API JSON 限制为 8 MiB，Release 附件同时检查声明大小和实际流式下载量，超限时删除部分文件；完整单元测试 `416 passed`。

### 2026-08-02 · REL-013 AI 网络响应内存预算

- OpenAI 兼容服务的普通 JSON、非 SSE 回退和流式 SSE 响应统一限制为 16 MiB，错误正文限制为 4 KiB，异常服务不能再让客户端无界读取到内存。
- 流式路径按接收字节累计并在超限时立即失败，既有重试、密钥脱敏和内容回调语义保持不变；完整单元测试 `413 passed`。

### 2026-08-02 · REL-012 朋友分享包完整性与输入预算

- `.gmshare` checksum 现在必须唯一、规范并完整覆盖包内全部载荷，篡改题目数据或遗漏校验条目会在写入题库前失败。
- 题目 JSONL 使用限长流式读取，并对 manifest、题目数、资源索引和资源引用执行类型、数量及交叉一致性校验；完整单元测试 `411 passed`。

### 2026-08-02 · REL-011 AI 审核批量决策查询去重

- AI 补全审核批量采用复用列表刷新时取得的并发状态，不再对同一建议的每个字段重复解析审核载荷和查询题目状态。
- 冲突字段仍会跳过，批量记录后仍从服务端刷新列表；完整单元测试 `409 passed`。

### 2026-08-02 · REL-010 本地文件夹云元数据完整性

- 共享目录中的仓库、同步清单、release 和设备 JSON 统一校验符号链接、8 MiB 大小、UTF-8、JSON 与根类型，并限制 release、资源和设备数量。
- 同步清单与设备列表改为唯一临时文件写入、刷新、`fsync` 后原子替换；设备 ID 同时通过安全路径组件校验。

### 2026-08-02 · REL-009 单资源存储预算

- 内容寻址对象库在哈希与复制前统一拒绝空文件和超过 128 MiB 的单资源，防止异常输入消耗磁盘 I/O 并在预览、导出或 AI 读取阶段放大内存占用。
- 录题、笔记、分享包、同步附件与派生题图共享同一写入边界，无需各流程重复实现限制。

### 2026-08-02 · REL-008 增量同步批次输入预算

- GitHub Operation 索引只接受固定资源名和合法 SHA-256，下载始终落到固定临时文件；限制批次索引、单批大小与记录数，并拒绝非法 UTF-8。
- 本地文件夹通道拒绝 `ops.jsonl` 符号链接，并限制设备目录数、文件大小和累计记录数，避免同步目录导致路径越界或资源耗尽。

### 2026-08-02 · REL-007 外部编辑工作区读取边界

- 工作区导入统一限制 manifest、元数据和 Markdown 的编码与大小，并拒绝题目目录、内容文件、资源目录及资源文件符号链接。
- 资源文件名必须安全解析在当前题目的 `assets/` 内，父目录穿越即使指向真实文件也会被拒绝；同时限制单工作区最多 10000 个题目目录。

### 2026-08-02 · REL-006 内容寻址对象写入完整性

- 对象复用前重新计算 SHA-256，拒绝同名哈希路径中的损坏或替换文件，避免错误原图继续进入预览、同步、导出与 AI 上下文。
- 新对象先写唯一临时文件，刷新并 `fsync`、复核哈希后再原子替换；失败路径清理临时文件，并拒绝越界目录或符号链接目标。

### 2026-08-02 · REL-005 对象库路径边界加固

- 主动安全审计发现资源路径解析未验证最终目标仍在内容寻址对象库，恶意同步/导入记录可能把预览、导出或 AI 读取重定向到库外文件。
- 资源解析现在拒绝父目录穿越、绝对/驱动器/ADS 路径、NUL、空路径、对象库根目录和经符号链接解析后的越界目标。
- 正常哈希对象、资源服务、增量同步和题目讨论裁剪读取保持兼容。

### 2026-08-02 · UI-201 导入导出与历史结果窗口收口

- 所有图片、文件夹、分享包、工作区、Word、备份、云恢复、资料合并和增量同步结果统一使用按操作类型标注的可访问窗口。
- 成功/失败窗口统一提供只读详情、复制、关闭和可选重试；重试关闭后调度，避免嵌套弹窗。
- 关闭结果后恢复触发控件焦点；若后台操作期间触发控件被禁用，则自动转移到下一可用控件。

### 2026-08-02 · UI-104 复习会话键盘提示

- 题目复习统一显示空格、数字 1–5、←/→ 的当前会话动作；笔记复习只显示 Enter 完成阅读，避免提示不可用操作。
- 可见提示与读屏描述完全一致，快捷键仅在复习会话页生效；答案披露、评分和笔记只读完成边界保持不变。

### 2026-08-02 · UI-103 复习计划详情与历史

- 选择计划后先展示有序内容预览、有效条目数和最近历史，只有单独点击“开始复习”才创建会话；空计划和已移除资料安全降级。
- 完整历史按轮次展示中文状态、题目数、已评分数与评分分布；笔记计划使用不可变阅读完成记录。
- 查看与关闭历史不改变当前计划、不创建会话，并保持复习计划构建器既有的排序和窄屏专注行为。

### 2026-08-02 · UI-116 AI 补全准备、执行与逐字段审核

- “AI 补全题目信息…”先进入页面级准备页，展示题目、模型、原图读取边界和费用提示；可选字段组与补充要求，点击“开始分析”前不创建任务。
- 执行阶段支持后台运行、取消和保留配置重试；前台完成自动审核，后台完成通知可准确返回任务，处理队列显示题目名、阶段和建议数。
- 审核按中文字段和内容分组，使用公式渲染的当前/建议对照；每项可采用、保留或编辑后采用，内部 ID、路径与裸 JSON 不进入普通流程。
- 最终按钮显示采纳字段数，服务端只应用明确采纳的字段；保留项不变，应用后刷新题目详情并保留审计、版本和撤销能力。

### 2026-08-02 · UI-114 题目讨论多区域框选引用

- 题目阅读区可切换到等比原图框选画布，多框和多原图引用使用归一化坐标，缩放后仍准确；支持移动、缩放、逐框删除、清空和键盘取消/切换。
- 提问前显示有序裁剪缩略图与页码，发送后清空本次选区；历史消息固化引用快照，重开会话时不会被后续选区修改。
- AI 请求同时保留整题结构化上下文并按顺序附带 PNG 裁剪；服务端拒绝跨题或失效页码引用，无原图旧题安全降级为整题文字提问。

### 2026-08-02 · UI-113 二级页面返回入口统一

- 题目、笔记、复习计划/会话、AI 录题各阶段统一使用标题左侧的圆角返回箭头及上下文可访问名称。
- 计划草稿和 AI 补全审核的文字返回按钮迁为箭头；AI 实时处理页移除重复“返回上传页”按钮，运行任务仍可从标题箭头安全返回。
- 完成页和复习完成页的返回动作属于流程结果主操作，按审计边界保留文字按钮。

### 2026-08-02 · UI-110 笔记与题库浏览管理骨架

- 笔记库统一题库的页面标题、搜索工具栏、Surface、分隔拖柄和底部批量操作节奏，双击笔记进入独立详情并保持原有浏览上下文。
- 760px 使用列表优先单栏，1366px/1920px 恢复导航与列表双栏；长标题省略并保留 Tooltip，列表无横向或重复纵向滚动。
- 正式、草稿、归档和回收站具有统一状态副标题和可访问说明；相关页面、服务与主题回归 `53 passed`。

### 2026-08-02 · UI-105 题目与 AI 讨论并排工作区

- 宽窗口提供题目/讨论可调双栏，760px 窄窗口切换为单栏题目/讨论模式；1366px 与 1920px 恢复双栏阅读。
- 打开讨论时聚焦提问框，关闭时恢复阅读焦点；按题目记忆阅读位置和当前会话，返回题库后仍可继续。
- 长会话完成渲染后自动滚至最新消息；相关 UI、对话和可访问性回归 `69 passed`。

### 2026-08-02 · UI-108 AI 录题结构化内容块闭环

- AI 录题确认支持有序文本、公式、表格和题图块，包含合并单元格编辑、块顺序调整、类型降级、来源图片顺序和归一化裁剪区域核对。
- 题图从不可变原图生成可追溯 `derived_figure` 裁剪；题库、详情、复习和 Word 导出按原题顺序展示结构化表格与裁剪图，Markdown 管道表格可安全降级渲染。
- 工作区、完整快照及 Operation 增量同步保留内容块；Operation 派生附件经过大小、类型、Base64、资源 ID 和 SHA-256 校验，不能覆盖原图。
- 完整 Windows 单元测试 `385 passed`，Ruff、Python 编译与差异检查通过。

### 2026-08-01 · UI-101 题库视图决策

- 正式维持高密度虚拟化列表方案，不新增独立卡片视图；题库继续优先支持搜索、复习和批量管理。

### 2026-07-31 · 多工作树 UI 与同步收口

- 完成笔记批量管理与内容块排序、复习计划选择/开始拆分、题库控件与树形展开样式、AI 完成通知及上传/确认页预览收口。
- 新增正式同步通道回归矩阵，覆盖 LocalFolder、GitHub 与完整快照的隔离测试入口。
- 完整 Windows 回归初测 `333 passed, 1 failed`；已修复安装包默认配置副本遗漏，受影响配置回归 `11 passed`。

### 2026-07-31 · UI-121 设置页耗时操作后台化

- AI/云端连接、索引维护、云端资料检查、备份恢复和增量同步迁移到后台任务，避免阻塞窗口。
- 相关入口防重；确认弹窗、结果窗口、冲突审核与成功/失败状态保持原有业务语义。

### 2026-07-31 · UI-120 设置字段级错误提示

- AI 模型、云端目录、仓库 owner/name 和测试连接令牌在对应输入项下方显示可操作的校验原因。
- 成功反馈继续使用非阻塞状态，危险确认及不可恢复失败仍保留原有窗口。

### 2026-07-31 · UI-124 区域重新识别候选去重

- 区域重新识别结果始终作为原候选的一份临时比较提案；不创建可独立入库的新候选。
- 连续重新识别时，最新提案会作废尚未决定的旧提案，过期结果不能覆盖当前候选。
- 回归覆盖保留原结果、采用新结果、重复提案与正式入库后的候选数量一致性。

### 2026-07-31 · UI-125 原图查看器缩放语义

- “重置缩放”恢复查看器打开时的适应窗口比例，不再意外放大到原始像素。
- 增加独立“原始大小”动作；切换来源图片后按当前画布重新适配。

### 2026-07-31 · UI-122 双栏结果对比缩放

- 区域重新识别的原结果与新结果会按各自对比栏宽度自动缩放，窄窗口优先完整展示内容。
- 回归 `34 passed`，Ruff、Python 编译与差异检查通过。

### 2026-07-31 · UI-123 题库展开行生命周期修复

- 延迟高度回调不再引用可能已销毁的 `QListWidgetItem`；滚动回收或刷新后的旧回调安全退出。
- 题库视图回归 `28 passed`，Ruff、Python 编译与差异检查通过。

### 2026-07-31 · UI-119 设置中心重构

- 设置改为“账户与设备、AI 服务、外观与显示、本地数据、云端同步”五栏目左侧导航；统一保存语义、凭据显隐、动态云端字段、状态反馈与防重。
- 完成最终视觉验收：760/1366/1920px、亮暗主题、单滚动条和 Tab 顺序。
- 剩余字段级错误提示与耗时操作后台化已拆为 UI-120、UI-121。
- 完整 Windows 单元测试 `319 passed`，Ruff、Python 编译与差异检查通过。

### 2026-07-31 · UI-003 局部样式与语义状态收口

- 目录危险菜单移除固定色值，改由全局亮暗主题 danger token 渲染；PDF 画布仅保留随当前主题动态取色的必要样式。
- 新增统一可访问状态组件，覆盖信息、加载、成功、失败、禁用和缺少配置/权限；加载骨架和错误重试组件补齐读屏说明。
- AI 模型列表明确显示加载、成功、空返回和失败，云端设置明确区分无需令牌、缺少凭据及凭据已保存，不改变底层业务校验和数据写入。
- 语义状态、设置页切换及亮暗主题均有回归覆盖。
- 完整单元测试 `317 passed`，Ruff、Python 编译与差异检查通过。

### 2026-07-31 · UI-002 低风险信息提示审计

- 题库和批量操作的未选择、空结果、已删除对象、无云端资料及清空回收站成功反馈改为非阻塞 Toast。
- AI 录题缺少作答图片/内容或未框选区域时在当前流程内说明；设置页 Mock/无需令牌/Faro 成功使用状态反馈；任务中心在摘要区提示未选任务或已有任务运行。
- 保留四类可停留信息框：两个 AI 异步任务完成入口、含 Provider 明细的云连接结果，以及必须选择处理方式的审核冲突；错误、高风险确认和带路径/合并明细的结果窗口同样保留。
- 回归测试确认迁移场景不会调用阻塞信息框。
- 完整单元测试 `309 passed`，Ruff、Python 编译与差异检查通过。

### 2026-07-31 · UI-001 详情与操作结果可访问性

- 题目详情补齐 `Alt+Left` 返回、完整读屏名称和显式焦点链；题目正文及 AI 对话的原生 PDF 阅读器成为有名称的键盘焦点目标。
- 待确认变更补齐列表说明、来源、字段差异、不确定项及操作按钮的读屏名称和 Tab 顺序。
- 导入、导出、本机备份恢复、云端资料恢复/合并和增量同步不再把结果压进普通消息框；统一结果窗口提供摘要、可复制只读详情、明确关闭按钮和失败重试入口。
- 原生 Windows UI Automation 实测可发现详情页的导航、编辑、前后题和正文阅读器，也可发现结果窗口的摘要、详情、重试与关闭控件。
- 完整单元测试 `307 passed`，Ruff、Python 编译与差异检查通过。

### 2026-07-31 · UI-008 AI 识别分阶段耗时复核

- AI 录题诊断补齐任务排队、缓存查找、分类目录匹配和后台线程到界面的信号交付耗时，原有图片编码、远端请求、响应解析、字段校验和候选写入指标继续保留。
- OpenAI 兼容 Provider 在响应可用时记录 token 用量以及 `Server-Timing`、处理毫秒等白名单服务端计时，不保存授权信息或任意响应头。
- 新增一条命令运行临时资料和本机 HTTP Provider，覆盖单图/多图、缓存未命中/命中和 503 自动重试；缓存命中断言 Provider 调用为 0，采样完成后资料已清理。
- 本机样本中 UI 信号等待为 `0.7–1.0ms`、分类匹配为 `2.6–5.9ms`、图片编码为 `0.1–0.3ms`；重试场景的 `612.4ms` 主要位于请求退避与等待，没有证据指向本地 UI 瓶颈。
- 完整单元测试 `305 passed`，Ruff、Python 编译与差异检查通过。

### 2026-07-31 · UI-007 AI 录题步骤条可读性修复

- 步骤节点按完整编号与名称的字体宽度计算最小尺寸，不换行、不参与剩余空间压缩；连接线保留 8px 下限并独占伸缩空间。
- 步骤条高度由 34px 调整为 44–48px，当前态内边距和边框不再上下裁切。
- 760、1366、1920px 实测节点均为 108px 完整宽度，连接线随容器扩展；读屏名称始终保留完整步骤名称。
- 主题与录题页面相关测试 `13 passed`，Ruff 与 Python 编译检查通过。

### 2026-07-31 · UI-006 内容渲染高度修复

- 定位到无窗口 Chromium 页面没有可用视口宽度，短内容在测量阶段逐字换行，使 `scrollHeight` 膨胀并生成近整页空白 PDF；外层 stretch 又放大了空白。
- 共享 `MathContentView` 在内容测量时固定与 A4 PDF 一致的 794 CSS px 宽度，并新增“短内容贴合、长内容到上限后内部滚动”的自适应高度模式。
- 题库展开、题目详情、AI 确认阅读预览和编辑字段预览统一接入；真实管线短题约 `344px` 且无滚动条，长题在 `420px` 上限内滚动且未裁切。
- 渲染与页面相关测试 `37 passed`，Ruff 与 Python 编译检查通过。

### 2026-07-31 · UI-005 识别缓存分类恢复

- AI 识别缓存升级为向后兼容的 v2 信封，完整保存规范化候选字段、不确定项、来源区域和受控结构建议，命中后不再丢失分类上下文。
- 同一图片首次识别与缓存命中的标题、科目、章节、新章节提案和核对理由保持一致；多候选及结构建议同样可回放。
- 本地目录 ID 或名称变化会改变包含完整目录提示的缓存键；重新识别仍返回失效章节时会移除 ID、保留有效科目并进入待确认。
- AI 与录题相关测试 `36 passed`，Ruff 与 Python 编译检查通过。

### 2026-07-31 · UI-004 题目内 AI 对话资源与重试修复

- 移除对不存在的 `Asset.sort_order` 的引用，只选择题目 `original` 资源并按创建时间和稳定 ID 排序，授权后支持确定性携带多张原图。
- 无图或原图文件失效时安全退回纯文本对话；旧的处理图等非原图资源不再被误带入模型请求。
- 同一失败提问再次发送时复用原消息并保留序号，成功后清除失败状态，不再让模型收到重复问题。
- 相关服务测试 `5 passed`，Ruff 与 Python 编译检查通过。

### 2026-07-31 · REL-002 Windows 正式安装包

- 新增 PyInstaller `onedir` 与 Inno Setup 6 每用户安装器，固定应用 ID 支持覆盖升级，默认程序目录与 `%LOCALAPPDATA%\Yancuo` 用户资料目录分离。
- 安装态自检覆盖 schema 迁移、默认 TOML 和协议 schema；移出源码仓库后的独立可执行目录验证通过，确认资源来自安装包内部。
- 新增隔离的安装—覆盖升级—卸载回归：升级和卸载后用户数据哨兵保留，卸载后程序文件删除，测试结束自动清理隔离资料。
- 新增版本标签/手动触发的 Windows 安装包工作流，构建并验证后上传 artifact；本机生成的 `0.1.0c1` 安装器完整回归通过。
- 完整单元测试 `292 passed`，Ruff 与 Python 编译检查通过。

### 2026-07-31 · REL-001 可重复性能基线与 UI-117 收口

- 新增一条命令完成隔离资料生成、性能采样和清理，固定覆盖 1200 道题目与 300 篇笔记，不读取或写入用户正式资料。
- 报告包含设备、Python/Qt/SQLAlchemy 版本、数据生成耗时、五次原始样本、中位数、范围和 Tukey 异常值；冷启动定义和未强制清空系统缓存的边界一并记录。
- 本机离屏基线中位数：冷启动 `675.56ms`、热启动 `58.86ms`、主窗口构建 `546.62ms`、题库刷新 `46.01ms`、本地搜索 `0.30ms`、题目/笔记列表查询 `23.62ms`/`9.47ms`、列表滚动往返 `13.87ms`。
- 题目删除入口正式移出“更多”菜单，危险按钮紧跟“编辑”，回收站视图不显示重复入口。
- 完整单元测试 `291 passed`，Ruff 与 Python 编译检查通过；隔离性能目录执行后已清理。

### 2026-07-30 · REL-000 总分任务书与问题流转体系

- 建立“问题收集箱 → 总任务书 → 方向分任务书 → 完成记录”的单向维护结构。
- 新增 UI、同步与云端、Android、发布与质量四个分任务书，并定义稳定任务 ID、状态和默认执行顺序。
- 将 13 份 schema 说明合并为一份数据库版本历史，将三份重叠 UI 规划收敛为 UI 分任务书和 UI 设计规范。
- 已完成路线图从当前文档树移除；产品架构、云端/Android 设计、工程决策和 Qt 故障复盘移入参考资料。
- 精简根 README，修正 Windows schema、Android 导入上限和 GitHub/GitLink 同步能力等过期描述。
- 本地 Markdown 链接检查通过；被删除资料仍可通过 Git 历史恢复。

### 2026-07-29 · 柔和视觉系统第七轮

- 使用 1200 道题目和 300 篇笔记的隔离资料完成真实业务量性能采样。
- 题库富文本行改为按视口实例化与回收，完整刷新由约 `457.93ms` 降至 `39.67ms`；主窗口构建由约 `1200.69ms` 降至 `865.00ms`。
- AI 录题确认页与题目编辑器补齐字段级读屏名称、说明和显式 Tab 顺序。
- 审核接受与今日复习评分的成功反馈改为非阻塞 Toast，错误、冲突和危险确认仍保留对话框。
- 真实 Windows 客户端加载 1200 条题目时仅实例化视口附近行；隔离验收资料已清理。
- 完整单元测试 `286 passed`，改动范围 Ruff 与 Python 编译检查通过。

### 2026-07-29 · 柔和视觉系统第六轮

- 设置、笔记、题库搜索和复习计划表单补齐显式 Tab 顺序及字段级读屏名称。
- 新增 `Ctrl+1`～`Ctrl+4`、`Ctrl+,` 顶层导航快捷键，以及题库/笔记 `Ctrl+K` 搜索聚焦。
- 普通设置保存、索引重建、撤销、加入复习、批量归类等成功反馈改为非阻塞 Toast；错误与高风险确认仍使用对话框。
- 真实 Windows 客户端完成快捷键、焦点链和 UI Automation 标签验收。
- 完整单元测试 `282 passed`，改动范围 Ruff、Python 编译与差异检查通过。

### 2026-07-29 · 柔和视觉系统第五轮

- 主导航恢复键盘焦点，支持方向键切换；核心树和列表补齐可访问名称、说明与焦点环。
- 纯文本大列表启用固定行高，并在批量刷新期间暂停逐项重绘；1500 行合成刷新约 `0.0066s`。
- 题库、笔记、任务中心和待确认变更补齐空结果或空任务说明。
- 修复 760px 窄窗口侧栏“可见但无法展开”的问题；选择页面后自动收回。
- 笔记页窄窗口改为“列表 + 正文”主视图，空间与合集按需披露，消除空状态文字和按钮裁切。
- 完整单元测试 `279 passed`，改动范围 Ruff、Python 编译与差异检查通过。

### 2026-07-29 · 柔和视觉系统第四轮

- 设置、云端同步与本机数据页接入统一语义 Surface，表单、卡片和滚动区不再依赖旧式硬边框。
- 任务中心、查重、待确认变更和原图查看器完成低频窗口迁移，补齐柔和工作区、工具条与操作栏。
- 完成浅色 100%、深色 125%、浅色 150% 真实 Windows 桌面验收；未改动系统缩放或原图预览缩放映射。
- 完整单元测试 `275 passed`，改动范围 Ruff、Python 编译与差异检查通过。

### 2026-07-29 · 柔和视觉系统第三轮

- AI 录题增加“上传图片—后台处理—确认结果”步骤条，三个阶段共享明确的完成、当前和待进行状态。
- 上传主区域、识别设置、补充说明、处理状态和确认结果分别使用主次 Surface；确认操作收束到独立底栏。
- 复习首页、计划选择和会话评分区使用统一柔和表面；题目与笔记会话正文进入最大 960px 的阅读画布。
- 复习计划构建器改为圆角工作区、透明栏间沟槽和内缩列表选择态，并保留窄窗口计划草稿模式。
- 完整单元测试 `274 passed`，本轮改动范围的 Ruff、Python 编译与差异检查通过。

### 2026-07-29 · 柔和视觉系统第二轮

- 新增主题感知 SVG 图标服务，提供普通、悬停/选中和禁用状态，并随亮暗主题切换刷新。
- 侧栏收放、题目展开、题库管理/更多操作、录题返回、笔记更多操作与搜索入口不再依赖字符箭头或系统杂项图标。
- 新增居中且最大宽度为 920px 的 `ReadingCanvas`，笔记正文在宽屏下不再横向铺满。
- 阅读画布保持纵向滚动，并移除 FitToWidth 模式下无意义的横向滚动条。
- 空笔记正文增加标题、操作提示、虚线承接面和次级背景，阅读内容与工作区背景形成稳定层级。
- 完整单元测试 `272 passed`，本轮改动范围的 Ruff 与 Python 编译检查通过。

### 2026-07-29 · 柔和视觉系统第一轮

- 全局主题增加语义表面、弱分隔线、焦点环和集中曲率指标，保留亮色与暗色成对定义。
- 新增抗锯齿内缩选择代理，题库树、处理列表、题目列表以及笔记合集/列表不再使用满宽矩形高亮。
- 题库和笔记工作区改为圆角外壳、分层表面与透明栏间沟槽，并完成真实 Windows 客户端检查。
- 业务服务、数据库、同步协议和页面行为保持不变。

### 2026-07-29 · AI 考研章节识别确认闭环

- AI 录题必须依据科目通用考试大纲判断章节，优先匹配本地完整章节路径。
- 本地缺少目标章节时，确认页显示规范章节建议、理由与置信度；用户接受后才创建，也可改选现有章节或保留未指定。
- 修复已有科目下的新章节提案在确认入库时被忽略的问题。
- 题库不再显示 0 题的“未分类”虚拟节点；存在未指定章节题目时改用带说明的筛选入口，归类完成后自动消失。

### 2026-07-25 · GitHub Operation 批次

- 定义不可变 JSONL 批次 Release 协议，索引仅在附件哈希校验成功后更新。
- GitHub 以专用 Release 标签实现保守写锁；竞争设备不能窃取或释放他人锁。
- `SyncService` 支持 GitHub 批次发布、下载、资料隔离和既有字段级冲突合并。
- 离线回归覆盖发布失败、重复拉取、附件篡改、损坏索引和跨资料隔离。
- 私有测试仓库真实联调通过：锁竞争、附件上传下载及哈希核对、发布中断恢复、两端批次推拉与同字段冲突审核。
- GitHub API 对 TLS 瞬态断连执行有限退避重试；GitLink 增量通道仍未完成。

### 2026-07-26 · GitLink 原子锁验证

- 在专用私有仓库中让两个客户端使用相同旧 `version_id` 顺序更新同一 Release；第二次旧版本写入被接受并覆盖第一次结果。
- 结论：`version_id` 是资源标识而非条件版本号，GitLink 不具备可复现的原子锁语义；增量 Operation 通道保持禁用，仅支持完整快照。

### 2026-07-25 · 本地资料接管与显式合并

- 资料快照携带稳定 `profile_id` 与谱系；可发现、恢复和绑定指定云端资料。
- 两份非空资料可逐字段选择本地/云端值，选择主资料后写入别名。
- 合并前创建不可变快照，合并结果不自动上传；搜索投影随后重建。

### 已完成能力摘要

- 本地录题、AI 识图审核、图片分组/复合题、知识目录、离线与受限 AI 搜索。
- 五档复习、题目详情、题目 AI 对话、独立块式笔记与笔记搜索。
- `.ebpack`、LocalFolder/GitLink/GitHub 完整快照、LocalFolder Operation 同步和 `.gmshare`。

## 后续维护规则

- 只在本文件新增可交付能力或当前基线变化；已完成实现细节写入对应协议、Schema 变更说明或 Git 提交。
- 每次改协议或 schema，先更新对应规范，再更新本文件的一行摘要。
- 当前未完成工作只维护在 [`任务书.md`](任务书.md) 及其链接的分任务书中。
